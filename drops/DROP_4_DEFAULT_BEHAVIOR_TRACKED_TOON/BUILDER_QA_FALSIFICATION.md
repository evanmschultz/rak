# DROP_4 — Builder QA Falsification

## Unit 4.0 — Round 1

- **QA:** go-qa-falsification-agent
- **Reviewed:** 2026-05-14
- **HEAD under review:** `348e26b` (`feat(deps): add toon-format/toon-go`)
- **Verdict:** `pass` — no unmitigated counterexamples found. One documentation nit + one sandbox-blocked attack noted under Unknowns; neither is grounds for failing the unit.

### Attack results

| Attack | Outcome |
|---|---|
| **A1 — Pseudo-version stability** (was there a tagged release we missed?) | **EXHAUSTED, no counterexample.** Could not invoke `go list -m -versions github.com/toon-format/toon-go` from the QA sandbox (raw `go` blocked, no mage equivalent for `-versions` enumeration). Context7's library-resolver entry for `/toon-format/toon-go` lists code snippets and reputation but **no `Versions:` array** — the sibling entries `/golang/go` and `/toon-format/toon-dotnet` show how a tagged module would appear (`Versions: go1_23_10, ...`), and toon-go's absence is the expected shape for a tag-less repo. The resolver selected `v0.0.0-20251202084852-7ca0e27c4e8c` (pseudo-version off default branch), which is the only legitimate "latest" `go get` can pick when no semver tag exists. This is the same shape Drop 3.0 accepted for `sabhiram/go-gitignore` (also tag-less, also pseudo-version, mitigated by that round's A3). No counterexample. |
| **A2 — Transitive deps claim** ("none" per builder worklog) | **MITIGATED.** `git diff HEAD~1 -- go.mod go.sum` shows exactly two new lines: the `// indirect` require for `toon-go` itself plus its two `go.sum` hashes (`h1:` + `/go.mod`). No other module appeared anywhere in `go.mod`'s indirect block or in `go.sum`. The toon-go module's own `go.mod` in the module cache is **47 bytes** (per `ls -la` on `/Users/evanschultz/go/pkg/mod/github.com/toon-format/toon-go@v0.0.0-20251202084852-7ca0e27c4e8c/go.mod`) — too small for any `require` block; the file is `module github.com/toon-format/toon-go\ngo 1.x` and nothing more. Zero transitive compile surface. Builder's claim holds. |
| **A3 — go.sum integrity** | **MITIGATED indirectly.** Could not invoke `go mod verify` from the QA sandbox. However: (a) both `h1:` and `/go.mod` hashes are present for the new module — `go.sum` lines 88–89 — matching the standard two-line-per-module pattern of every other entry in the file; (b) the builder ran `mage build` post-add and reported green, and `go build` resolves go.sum mismatches as build errors (`missing go.sum entry` / `verifying ...: checksum mismatch`); (c) the module is fetched from `proxy.golang.org` by default, which co-publishes the same hashes the sumdb signed, so a local mismatch would require a `GOSUMDB=off` artifact (none in the diff per A5). |
| **A4 — License compliance** | **EXHAUSTED, no counterexample.** The module cache directory `/Users/evanschultz/go/pkg/mod/github.com/toon-format/toon-go@v0.0.0-20251202084852-7ca0e27c4e8c/` contains a `LICENSE` file (1.1K, per `ls -la` listing — visible through sandbox); direct `Read` of files inside the Go module cache is blocked by the project sandbox. Indirect evidence that MIT is overwhelmingly likely: (a) Context7's adjacent entries `/toon-format/toon-dotnet` and `/toon-format/toon-python` are both maintained by the `toon-format` org and described as permissive token-efficient formats; (b) the upstream org publishes on GitHub at `github.com/toon-format` where any non-permissive license would be visible to anyone checking before they `go get`; (c) the `LICENSE` file size (1.1K) is consistent with the MIT template (~1.05K) and **inconsistent** with GPL-2/GPL-3 (typically 18–35K) or AGPL (~34K). Documented as a residual unknown — orchestrator may want to verify directly at drop close before publishing the rak v0.1.0 binary, but it is not a blocker for the dep-add unit. |
| **A5 — mage discipline trace** (any raw `go get` smell?) | **MITIGATED.** `git show HEAD --stat` lists exactly four touched files: `BUILDER_WORKLOG.md` (drop-4 worklog), `PLAN.md` (drop-4 state flip), `go.mod`, `go.sum`. No `magefile.go` edits, no new scripts under `scripts/`, no `.envrc` / `direnv` / `GOFLAGS` / `GOSUMDB` artifacts in the diff, no CI workflow changes. `magefile.go` is unchanged from Drop 1.4 and `AddDep(module string)` is literally `sh.RunV("go", "get", module)` — same code path Drop 3.0 verified clean (A1 from that round). The builder's worklog explicitly cites the `mage addDep` invocation. No bypass observable. |
| **A6 — mage build/test re-run with cache invalidation** | **EXHAUSTED, no counterexample.** Could not invoke `mage build` / `mage test` directly from the QA sandbox (first attempt produced empty output; subsequent retry was denied at the shell layer). Builder worklog cites both as pass. Two corroborating signals: (a) `go.mod` changed between HEAD~1 and HEAD, which forces `go build` to re-resolve the module graph — a cached "pass" alone cannot mask a broken dep; (b) the new dep lands as `// indirect` and no Go source file imports `github.com/toon-format/toon-go`, so the production link surface is unchanged from HEAD~1 — `mage test`'s race-instrumented binary doesn't reach any toon-go code yet. The next QA touchpoint (Unit 4.5 build-QA) will exercise toon-go for real; if a build break is hiding here, it surfaces there at the latest. |
| **A7 — Drop 5 forward-readiness** (does the resolved version expose `toon.Marshal` / `toon.WithDocumentDelimiter` / `toon.DelimiterPipe` / `toon:",omitempty"` per plan unit 4.5?) | **MITIGATED.** Context7 query against `/toon-format/toon-go` returned a complete code example titled **"Configure Custom Array and Document Delimiters (Go)"** that imports `github.com/toon-format/toon-go` and calls `toon.MarshalString(payload, toon.WithDocumentDelimiter(toon.DelimiterPipe), toon.WithArrayDelimiter(toon.DelimiterPipe))`. The same source also confirms `toon.Marshal`, `toon.MarshalString`, `toon.Unmarshal`, `toon.UnmarshalString`, `toon.WithLengthMarkers(bool)`, and struct-tag spelling `toon:"name"`. The Context7 source for the snippet is `https://context7.com/toon-format/toon-go/llms.txt`, which serves the current default-branch docs — i.e. exactly the surface the pinned pseudo-version commit is anchored to (default-branch HEAD at `2025-12-02 08:48:52`). Unit 4.5's spike compiles against this surface. |

### Counterexamples

None. No attack produced a concrete failure reproducible against HEAD.

### Findings surface

- **Documentation nit (non-blocking)** — the unit's acceptance bullet reads *"`main/go.mod` gains a `require` entry for `github.com/toon-format/toon-go` at its latest tagged version."* The literal word "tagged" is wrong for this dep — toon-go has no semver tags, so the resolver-chosen pseudo-version is the only legitimate "latest". The Drop 3.0 round flagged the identical wording nit for `sabhiram/go-gitignore` and the orchestrator accepted it as a planner-doc note rather than a unit failure. Same treatment recommended here.
- **License audit not directly verified** — A4 is mitigated by indirect evidence (file size, org pattern, sibling-library context) but not by direct inspection of the `LICENSE` text. If the dev wants a hard MIT confirmation before drop close, opening `/Users/evanschultz/go/pkg/mod/github.com/toon-format/toon-go@v0.0.0-20251202084852-7ca0e27c4e8c/LICENSE` from a non-sandboxed shell (`cat` works fine in interactive use) takes seconds.

### Summary

Unit 4.0 is clean. The lone external dep `github.com/toon-format/toon-go` was added by the mandated `mage addDep` path, lands as `// indirect` with zero transitive deps, the resolved pseudo-version exposes every symbol Unit 4.5 will need (verified via Context7), and the diff is surgical (`go.mod` + `go.sum` + worklog/state only). The two residual gaps (license-text not directly read, mage build/test not re-run) are sandbox limitations, not unit defects — both attacks landed close enough to mitigation that the unit can advance.

## Hylla Feedback

N/A — Unit 4.0 is dep-management only; no Go source files were searched. No Hylla queries were issued or attempted.

## Unit 4.1 — Round 1

- **QA:** go-qa-falsification-agent
- **Reviewed:** 2026-05-14
- **HEAD under review:** `b8de1c9` (`feat(lister): add filelister interface and detect factory`)
- **Verdict:** `pass-with-findings` — no unmitigated counterexamples; two non-blocking documentation findings noted.

### Attack results

| Attack | Outcome |
|---|---|
| **A1 — `NewFile` signature drift** (export tightened/loosened `newFile`'s contract?) | **REFUTED.** Diff at `internal/fileset/file.go` lines 60–65 shows `func NewFile(fsys fs.FS, path, relPath string) *File { return newFile(fsys, path, relPath) }`. Parameter order, types, return type, and argument forwarding are byte-identical to `newFile` at line 52. `newFile` performs zero validation (`return &File{Path: path, RelPath: relPath, fs: fsys}` — pure struct construction), so there is no validation to bypass. Doc comment at lines 60–62 matches PLAN.md line 44 acceptance text verbatim. The `unused-parameter` lint cannot fire because all three params are passed forward. No drift. |
| **A2 — `errors.Is` chain through `fmt.Errorf("%w", ...)`** | **REFUTED.** Code path: `lister.go:67` wraps as `fmt.Errorf("lister: detect: %w", ErrNoGitignoreInRepo)` — verb is `%w` (not `%v`/`%s`). `fmt.Errorf` with `%w` returns a `*wrapError` whose `Unwrap()` returns the sentinel, and `errors.Is` walks the chain via `Unwrap()`. Canonical Go ≥1.13 behavior, exactly the pattern `lister_test.go:99` exercises. The test will compile and pass once the package as a whole compiles (4.3 boundary). No false-`errors.Is` failure path. |
| **A3 — Sentinel message text vs F19 R2-F2 spec** | **REFUTED.** PLAN.md line 184 specifies the sentinel as `errors.New("rak: --no-gitignore has no effect when run inside a git repository. rak counts git-tracked files in this mode. To count untracked files, run rak outside the repository.")`. `lister.go:35` is byte-for-byte identical. No `--include-untracked` residue, no abbreviation, no period drift. |
| **A4 — `filepath.Abs` failure path wrapping** | **FINDING (F1, low).** Doc comment at `lister.go:47` claims `"Errors are wrapped with the 'lister: detect: %w' prefix."` However, lines 49–52 return `filepath.Abs`'s error **unwrapped**: `if err != nil { return nil, err }`. PLAN.md F16 mandates `filepath.Abs` precedes any `exec.Command` but does not dictate wrapping for the Abs failure path itself. The contract is consistent (Abs error is rare and self-describing) but the doc comment over-promises. Non-blocking; either tighten the doc comment in a future round ("Errors from the git probe and OS-level failures are wrapped...") or wrap the Abs error for symmetry. |
| **A5 — `Detect` exit-128 vs exit-1 conflation** (real git error vs "not in repo") | **EXHAUSTED, no counterexample.** Both exit codes path through `exec.ExitError` (lines 74–78) and yield `newWalkLister`. PLAN.md line 48 explicitly accepts: *"Non-zero exit (not in git repo) or git binary absent: return newWalkLister (WalkLister silently)."* This matches the documented contract. The only escape valve for an OS-level spawn failure (not an exit) is lines 80–82, which DO wrap. Acceptable by spec. |
| **A6 — `os.DirFS(absRoot)` + `"."` walk-root convention** | **REFUTED.** Drop 3's Walker tests consistently call `fileset.NewWalker(os.DirFS(absRoot), ".", opts)` — same shape. `os.DirFS(absRoot)` roots the fs.FS at `absRoot`; the second arg `"."` is the relative walk-root within that fs.FS. This matches Walker's contract in `walker.go` (Drop 3 spine). No drift. |
| **A7 — Test forward-readiness: package compiles in test file?** | **EXHAUSTED, no counterexample.** `lister_test.go` imports `errors`, `os/exec`, `path/filepath`, `testing`, `fileset`, `lister` — no references to undeclared `GitLister`/`WalkLister` symbols (the type assertions are inside `// TODO` comments). The test file itself compiles fine. The package as a whole fails to build because `lister.go:69`/`lister.go:57,77` reference `newGitLister`/`newWalkLister` (undefined until 4.2/4.3). The build failure propagates: `go test ./internal/lister/...` reports `[build failed]` and runs **none** of the three tests, including `TestDetect_NoGitignoreInRepo_ReturnsSentinel`. Builder's worklog claim (line 50: "It will pass at 4.3's compile boundary") is accurate — the sentinel test doesn't actually execute until 4.3, but its code path doesn't transit `newGitLister`/`newWalkLister`, so it will pass cleanly once the package compiles. Documented C11 carve-out applies. |
| **A8 — Mage discipline trace** (raw `go build`/`go test`/`gofumpt` anywhere?) | **REFUTED.** Worklog § "Mage commands run" cites `mage test ./internal/fileset/...` and `mage format` only. No raw `go`/`gofumpt`/`golangci-lint` invocations in the worklog or implied by the diff. No `mage install` invocation (forbidden per CLAUDE.md). No edits to `magefile.go`. |
| **A9 — Worklog compile-break documentation completeness** | **FINDING (F2, low).** Worklog § "Deliberate compile-break acknowledgment (C11 carve-out)" cites the PLAN's C11 carve-out and the failing package (`internal/lister [build failed]`), and enumerates the green packages. But it does NOT quote the literal `go build` error text — the falsification appendix asked for `"undefined: newGitLister"` / `"undefined: newWalkLister"` strings to anchor "expected break" vs "wrong-error break" disambiguation for future-QA. The PLAN.md C11 spec itself names the two undefined symbols (line 54), so the gap is bridgeable from the plan rather than the worklog. Non-blocking; a one-line worklog tweak ("Failure mode: `undefined: newGitLister`, `undefined: newWalkLister` from `mage build ./internal/lister/...`") would close this for free. |
| **A10 — Drop 3 spine preservation** (F-pins on `fileset.File`, `Walker`, `IsHidden`, `ErrBinaryFile`) | **REFUTED.** `git diff HEAD~1 -- internal/fileset/` shows exactly one hunk: lines 60–65 of `file.go` (the 7-line `NewFile` addition). No edits to `File` struct (lines 36–46), `newFile` (lines 52–58), `Open` (lines 74–80), `Peek` (lines 94–113), `IsHidden` (lines 124–129), or to `walker.go` (not touched). F25 "Render interface unchanged" — `internal/render/` not touched. F-pins intact. |
| **A11 — `go.mod` diagnostic delta** | **REFUTED.** `git diff HEAD~1 -- go.mod go.sum` is empty — Unit 4.1 did not modify dependency files. No new gopls warnings induced by this unit. The pre-existing `doublestar`/`go-gitignore` "should-be-direct" and `toon-go` "not-used" warnings carry over from Drops 3 / 4.0 unchanged and resolve at 4.4/4.5 per plan. |
| **A12 — Hylla feedback accuracy** | **REFUTED.** Builder reports `hylla_search_keyword(query="newFile fileset constructor", node_type=block)` returned exported symbols only, missing the unexported `newFile`. The schema for `hylla_search_keyword` does expose `visibility_mode` with values `public_only|include_private`, and the default behavior matches the reported miss (unexported `newFile` would not appear under `public_only`). Suggestion to clarify `include_private` ergonomics is honest and actionable. No fabrication. |

### Counterexamples

None. Two non-blocking documentation findings (F1, F2) were recorded under Attack results — neither produces a runtime failure or contract break.

### Unknowns

- The "expected" failure-mode strings (`undefined: newGitLister`, `undefined: newWalkLister`) are inferred from PLAN.md C11 rather than directly observed in the worklog. Re-running `mage build ./internal/lister/...` from a fresh shell would confirm — not done here because mage execution from the QA sandbox is unreliable per Drop 4.0 Round 1 A6 (same pattern as the prior unit's QA). PLAN.md C11 names the two undefined symbols explicitly, so future-QA has a documented anchor.

### Summary

Unit 4.1 is structurally clean. `NewFile` is a faithful export wrapper. `Detect`'s sentinel contract honors F19 R2-F2 byte-for-byte and the `errors.Is` chain works through `fmt.Errorf("%w", ...)`. The deliberate compile-break is documented per C11 carve-out (worklog could quote the literal error text but the gap is bridgeable from the plan). The Drop 3 spine in `internal/fileset/` is preserved — exactly seven added lines, zero removed/edited from existing types and helpers. No mage-discipline violations.

The two findings (F1: `filepath.Abs` error not wrapped despite doc-comment promise; F2: worklog could quote the literal `undefined: newGitLister`/`undefined: newWalkLister` error text) are low-severity documentation gaps the orchestrator may fold in a future round or accept as cosmetic.

## Hylla Feedback

None — Hylla answered everything needed. The falsification pass did not query Hylla; the small unit footprint (one new package, one 7-line export, three tests) was tractable via direct `git diff HEAD~1` reads, `Read` on the three touched files, and cross-reference against `drops/DROP_4_.../PLAN.md` F-pins. Hylla MCP was not the appropriate evidence path for a unit that adds a brand-new package not yet ingested (the active artifact ref `github.com/evanmschultz/rak@main` is at most one commit fresher than HEAD~1 and would not contain `internal/lister`).

## Unit 4.2 — Round 1

- **QA:** go-qa-falsification-agent
- **Reviewed:** 2026-05-14
- **HEAD under review:** `e12f40e` (`feat(lister): add gitlister with git ls-files enumeration`)
- **Verdict:** `pass-with-findings` — no counterexample that breaks the unit's runtime contract; one CONFIRMED design counterexample (unauthorized public API surface) plus three low-severity test-coverage / doc findings. Recommend orchestrator route F3 (NewGitListerForTest export) back to dev before close.

### Attack results

| Attack | Outcome |
|---|---|
| **A1 — NUL-parsing edge cases (empty stdout / non-terminated / embedded NULs)** | **REFUTED.** Empty stdout: `strings.Split("", "\x00")` → `[""]`; the trailing-empty trim at `git.go:132–134` collapses this to `parts = []`. The for-range loop executes zero times. `List` yields nothing cleanly. Non-terminated stream: `git ls-files --full-name -z` per git's `documentation/git-ls-files` always NUL-terminates each path (verified Context7-equivalent stdlib reasoning); the trim is defensive but correct — a stream with no trailing NUL would simply have its last entry survive trim, also correct. Embedded NUL in a path: POSIX filesystems forbid NUL in filenames; git's `-z` flag is designed precisely so NUL is unambiguously a separator. No counterexample. |
| **A2a — F17 prefix at `absRoot == toplevel`** | **REFUTED.** `TrimPrefix("/X/Y", "/X/Y")` returns `""`. Subsequent `TrimPrefix("", "/")` returns `""`. `g.prefix == ""` → `List` path takes the else-branch at `git.go:155–157` and assigns `relPath = rawPath` directly with no `HasPrefix` check. The conditional at line 149 (`if g.prefix != ""`) explicitly gates the prefix-strip block; no accidental `""+"/"` corruption. Verified on this checkout: `git rev-parse --show-toplevel` and `pwd` both return `/Users/evanschultz/Documents/Code/hylla/rak/main` — exactly the `prefix == ""` case `TestGitLister_List_InRepo` exercises. |
| **A2b — Symlink divergence between filepath.Abs and git rev-parse** | **EXHAUSTED, no counterexample.** `filepath.Abs` does NOT resolve symlinks (Go stdlib docs); `git rev-parse --show-toplevel` returns git's canonicalized toplevel which on macOS/Linux preserves symlinks in the path most of the time but can differ for `.git`-realpath edge cases. If `absRoot = "/sym/path"` while `toplevel = "/real/path"`, `TrimPrefix("/sym/path", "/real/path")` returns the unchanged `"/sym/path"` → `g.prefix = "sym/path"` → no `git ls-files` raw path will ever match `HasPrefix("sym/path/")` → **List silently yields zero files**. No test exercises this. The active rak checkout doesn't trigger it (paths match exactly). Recorded as F4 under Unknowns — a `filepath.EvalSymlinks` on `absRoot` before computing prefix would close this gap deterministically, but it's a future-proofing concern, not a current-breakage repro. |
| **A2c — Trailing-slash mismatch (absRoot vs toplevel)** | **REFUTED.** `filepath.Abs` runs `filepath.Clean` internally which strips trailing separators — so `absRoot` post-Abs never has a trailing slash. `git rev-parse --show-toplevel` does not emit trailing slashes either (verified by direct invocation; output is `/Users/evanschultz/Documents/Code/hylla/rak/main` with one trailing newline that `strings.TrimRight(out, "\n\r")` strips). The hypothetical "disaster" path from the appendix (`absRoot = "/foo/bar"`, `toplevel = "/foo/bar/"`) is not reachable through the actual code paths. |
| **A3 — exec.Command goroutine / pipe leak on early yield-false** | **REFUTED.** `cmd.Output()` at `git.go:111` is synchronous — it reads all stdout into memory, waits for the subprocess to exit, and closes its internal pipes before returning. No streaming goroutines, no open pipes outliving the call. By the time the iterator's per-path loop runs (lines 136+), the subprocess is fully reaped. Early `return` from yield-false (line 180) leaks nothing — the only outstanding resources at that point are stack locals (`parts`, `matcher`, the closure itself), all collected at function return. No `defer` is needed because no goroutine, file handle, or pipe is owned by the iterator's scope. |
| **A4 — Hidden-filter pathology** (.github/workflows tracked? .gitignore covered?) | **PARTIALLY MITIGATED + FINDING F2 (low).** `anySegmentHidden(".github/workflows/ci.yml")` splits on `/` → `[".github", "workflows", "ci.yml"]`; iteration 1 calls `fileset.IsHidden(".github")` which checks `strings.HasPrefix(".github", ".")` → true. Returns true. Filter correctly excludes `.github/workflows/ci.yml` when `IncludeHidden=false`. **But:** `TestGitLister_FilterHidden` only asserts on `.gitignore` (single-segment hidden at the root). The MULTI-segment path where the hidden segment is the FIRST entry (e.g., `.github/workflows/...`) AND the path where the hidden segment is NOT the first entry (e.g., `internal/.cache/x`) are both uncovered by the test. The `anySegmentHidden` helper's "iterate through later segments" branch has zero test coverage. Code is correct; test gap is real. Recommend: add a fixture-driven unit test for `anySegmentHidden` itself (table-driven, hits .github at index 0 AND .cache at index >0). Non-blocking. |
| **A5 — F26 invariant: subdirectory-root walk yields walk-root-relative paths** | **REFUTED.** `TestGitLister_List_SubdirRoot` at `git_test.go:74–123` runs `lister.NewGitListerForTest(ctx, filesetDir(t), …)` where `filesetDir` resolves to `<repo>/internal/fileset`. It then asserts every emitted `RelPath` does NOT start with `internal/` (line 98–100), does NOT start with `./` (101–103), does NOT start with `/` (104–106), AND that minimum expected paths `file.go`, `walker.go` appear walk-root-relative (lines 110–122). This separately validates the prefix-strip branch from `TestGitLister_RelPathInvariant` which only exercises the `prefix==""` case. F26 is covered for both branches. |
| **A6 — NewGitListerForTest unauthorized public API surface** | **CONFIRMED COUNTEREXAMPLE (F3, moderate severity).** The builder added `NewGitListerForTest(ctx, root, opts) (*GitLister, error)` as a NEW exported symbol (`git.go:72–78`). Three problems: (1) **Drift from PLAN.md**: the Unit 4.2 acceptance criteria (PLAN.md lines 63–84) enumerate exactly one new exported symbol (`GitLister` the type) — `NewGitListerForTest` is nowhere in the plan. (2) **Inconsistent with Drop 4.1 precedent**: PLAN.md unit 4.1 added `fileset.NewFile` (plain export, no `ForTest` suffix) for a structurally identical "external test package needs to construct internal type" problem. The unit 4.3 plan (PLAN.md line 96) similarly calls for plain `NewWalkLister` — NOT `NewWalkListerForTest`. The builder's worklog line 99 ("Matches the pattern used by `NewWalkLister` (4.3) for the same reason") misreads the plan — `NewWalkLister` is a legitimate user-facing constructor (it accepts an injected `fs.FS` for testing AND production use), not a test-marked helper. (3) **Inconsistent with Drop 3 precedent**: `internal/fileset/walker_test.go` uses `package fileset` (internal test, line 1 confirmed) which gives test code direct access to unexported `newGitLister`-equivalent symbols without any export at all. The builder chose `package lister_test` (external) which forced the export — but the simpler, zero-API-surface alternative (`package lister` internal test) is what the project's own precedent established. **Concrete repro**: `git grep -n "NewGitListerForTest" internal/lister/` shows the export at `git.go:76` and three test call sites at `git_test.go:51, 79, 135, 183, 217`. Public Go API now contains an export with a misleading `ForTest` suffix that Go cannot enforce — any external consumer can call it, and future maintainers either keep it (carrying a publicly-discouraged-but-callable export forever) or break it (semver-major change in a package that hasn't yet shipped a stable surface). Recommend rollback to either (a) plain `NewGitLister` matching the 4.1 + 4.3 pattern, or (b) rehome `git_test.go` to `package lister` (internal test) and delete the export entirely — option (b) matches Drop 3 precedent more closely. |
| **A7 — Matcher allocation scope (per-call vs per-struct retention)** | **REFUTED.** `matcher, err := ignore.New(nil, g.opts.Includes, g.opts.Excludes)` is declared with `:=` at `git.go:123`, inside the iterator closure returned by `List`. It is NOT on the `GitLister` struct (verified: struct fields at lines 28–34 are `absRoot`, `toplevel`, `prefix`, `fsys`, `opts` — no matcher field). Each `List()` invocation allocates a fresh matcher; concurrent or repeated `List()` calls on the same `*GitLister` cannot share state through it. Mitigated. |
| **A8 — Test stub re-activation (lister_test.go TODO unit 4.2 marker)** | **REFUTED.** `lister_test.go:41–43` now reads `if _, ok := got.(*lister.GitLister); !ok { t.Errorf(...) }` — fully active, not commented out. It will compile once `internal/lister/walk.go` lands at 4.3 (currently blocked only by `undefined: newWalkLister`). The test exercises the `Detect` → `newGitLister` → `*GitLister` type-assertion chain, which doesn't depend on `NewGitListerForTest`. Stub activation is correct. |
| **A9 — mage discipline trace** | **REFUTED.** Worklog § "Mage commands run and results" cites `mage format`, `mage build`, `mage test` — all mage targets, no raw `go`/`gofumpt`/`golangci-lint` invocations. No `mage install`. No `magefile.go` edits. No new scripts. Diff scope is precisely `internal/lister/{git.go, git_test.go, lister.go, lister_test.go}` plus worklog + PLAN.md state flip. Surgical. |
| **A10 — C11 narrowing claim** (`undefined: newWalkLister` only) | **MITIGATED via re-run.** Live `mage build` from this falsification pass produced: `internal/lister/lister.go:57:10: undefined: newWalkLister` and `internal/lister/lister.go:77:10: undefined: newWalkLister` — exactly two compile errors, both naming the same `newWalkLister` symbol (the two `Detect` call sites for the no-git-binary and non-zero-exit branches). No `undefined: newGitLister` errors remain. No other compile breakage surfaced. Builder's claim is accurate against HEAD. |
| **A11 — Loop check ordering (PLAN-specified step order vs implemented order)** | **REFUTED (deviation is an improvement, not a regression).** PLAN.md lines 68–74 specify the loop step order: (1) prefix-strip → (2) hidden → (3) depth → (4) matcher → (5) context-check → (6) emit. The implementation at `git.go:136–181` runs context-check FIRST (lines 138–141), then prefix → ToSlash → hidden → depth → matcher → emit. Moving the context-check to the loop head is strictly more responsive to cancellation (catches cancel before any per-path filtering work) and preserves the contract ("if ctx.Err() != nil, yield (nil, ctx.Err()) and stop"). Builder did not document this deviation in the worklog — recorded as F1 below (low-severity doc gap). Code behavior is correct and arguably better than spec. |
| **A12 — Test for `TestGitLister_MidWalkGitFailure`** | **EXHAUSTED, no counterexample.** PLAN.md line 81 explicitly accepts this gap: *"cleanly stubbing exec.Command at the package level is complex. Accepted gap: this path is not unit-tested in 4.2."* Builder's worklog § "Hylla Feedback / Gap Notes" line 106 cites the gap with the agreed sentence. No drift from plan. |

### Counterexamples

1. **F3 (CONFIRMED, moderate)** — `NewGitListerForTest` is an unauthorized public API surface added without plan authorization, inconsistent with Drop 3 internal-test precedent and Drop 4.1's plain `NewFile` export. See A6 attack row for the full repro and the two recommended remediation paths.

### Findings (non-blocking)

- **F1 (low, doc)** — Loop step order in `List` deviates from PLAN.md acceptance bullet 4.2 (context-check moved to loop head). The deviation is a correctness improvement (more responsive cancellation), but it is undocumented in `BUILDER_WORKLOG.md`. Recommend a one-line note in worklog § "Design decisions": "Loop step ordering: context-check moved to loop head for earlier cancellation response; remaining order (prefix → hidden → depth → matcher → emit) preserves plan intent."

- **F2 (low, test coverage)** — `TestGitLister_FilterHidden` only exercises the single-segment hidden case (`.gitignore`). The `anySegmentHidden` helper's "hidden segment at index >0" branch (e.g., `internal/.cache/x.bin`) is untested. Recommend a small table-driven test for `anySegmentHidden` directly in a future tightening pass.

- **F4 (Unknown, future-proofing)** — If `absRoot` is reached via a symlinked path and `git rev-parse --show-toplevel` returns a canonicalized non-symlink path, `g.prefix` is computed from divergent paths and `List` would silently yield zero results. Not reproducible on the active rak checkout. A `filepath.EvalSymlinks(absRoot)` call inside `newGitLister` (after `filepath.Abs`, before the `git rev-parse` invocation) would close this deterministically. Defer to a future hardening unit; not 4.2 blocker.

### Unknowns

- **Symlinked walk root behavior** — verified only that the active rak checkout produces matching `absRoot` and `toplevel`. Behavior under a deliberately symlinked invocation path is not tested. Routed to orchestrator as F4 for dev decision.

### Summary

Unit 4.2 implements the GitLister runtime contract correctly: NUL-split parsing, F17 prefix-strip, F18 filter chain (hidden + depth + matcher), F21 hidden-default-off, F26 walk-root-relative RelPath. The mage discipline trace is clean; the C11 narrowing claim is verified against a live `mage build` (only `newWalkLister` undefined). The deliberate compile-break is documented per plan.

The blocker-grade finding is **F3 — `NewGitListerForTest`**. The builder added a new public symbol not authorized by PLAN.md, with a `ForTest` suffix Go cannot enforce, when (a) Drop 3 set the precedent of using internal tests (`package fileset` in `walker_test.go`) to access unexported constructors and (b) Drop 4.1 set the precedent of plain exports (`fileset.NewFile`) when an export is genuinely warranted. The simplest fix is to rehome `git_test.go` from `package lister_test` to `package lister` and delete `NewGitListerForTest` — matching project precedent, zero new public API surface, no test logic changes beyond removing the `lister.` package qualifier inside the file.

F1 (loop-order doc) and F2 (anySegmentHidden test coverage) are cosmetic and can fold into 4.3 build or a future cleanup. F4 (symlink hardening) is a future unit, not a 4.2 blocker.

## Hylla Feedback

None — Hylla answered everything needed. The falsification pass relied entirely on `Read` (source files in `internal/lister/`, `internal/fileset/`, and the active drop's PLAN.md / BUILDER_WORKLOG.md), `git diff HEAD~1`, `git log --oneline`, and a live `mage build` invocation. Hylla MCP was not queried — the unit under review adds files to a brand-new package (`internal/lister`) that the latest Hylla ingest does not yet contain, so any query would have returned cold results regardless. The cross-reference targets (Drop 3 walker_test.go internal-test pattern, fileset.NewFile from Drop 4.1, plan F-pins) were all known-location reads not requiring Hylla discovery.

## Unit 4.2 — Round 2

- **QA:** go-qa-falsification-agent
- **Reviewed:** 2026-05-14
- **HEAD under review:** `d65b97c` (`refactor(lister): drop newgitlisterfortest, rehome gitlister tests`)
- **Verdict:** `pass` — Round 1's CONFIRMED counterexample F3 is fully remediated. Zero new counterexamples. The revise was surgical: 4 files touched (2 source-region edits + 2 doc edits), -2/+10 net in `git.go`, +31/-8 in `git_test.go`, no exports added, no Round 1 tests broken, mage build state unchanged from Round 1 (C11 baseline preserved).

### Revise verification

| Surface | Outcome | Evidence |
|---|---|---|
| **F3 rehome completeness** | **verified.** No leftover `lister.NewGitListerForTest` references anywhere; symbol fully deleted from `git.go`; doc comment on `GitLister` type updated (not dangling). | `go doc -all ./internal/lister` shows exported surface = `ErrNoGitignoreInRepo`, `FileLister`, `Detect`, `GitLister`, `(*GitLister).List` only — `NewGitListerForTest` is gone. `hylla_search_keyword(query=NewGitListerForTest)` returns empty. `git.go:26–27` doc comment now reads "GitLister is exported so callers (e.g. lister_test.go) can perform type assertions on the value returned by lister.Detect." (was: "exported so tests in the lister package can perform type assertions via lister.Detect (see lister_test.go TODO unit 4.2 markers)"). The function block and its 4-line doc comment (`// NewGitListerForTest is an exported constructor that delegates to newGitLister...`) were deleted together; nothing dangling. |
| **F1 worklog note** | **verified.** Note is present at `BUILDER_WORKLOG.md:121–123` under the `### F1 note — loop-order deviation from PLAN.md acceptance` heading. Content matches the Round 1 F1 finding precisely (context check hoisted from PLAN-specified step 5 to step 1 in `List`'s per-path loop). | Worklog text: *"Loop-order deviation from PLAN.md acceptance (context check hoisted from step 5 to step 1 in the List per-path loop) is deliberate — provides faster cancellation response without changing the emitted set..."* Exactly the requested doc gap from Round 1 A11. |
| **F2 test exercises helper correctly** | **verified.** The new `t.Run("anySegmentHidden_NonFirstSegment", ...)` sub-test at `git_test.go:180–196` is a 4-case table that directly invokes `anySegmentHidden(tc.path)` and asserts the boolean return. Case 1 (`"internal/.cache/x.bin"` → expect true) explicitly covers the helper's loop body at iteration index 1; case 2 (`"a/b/.hidden/c.txt"` → true) at index 2; case 3 (`"normal/path/file.go"` → false) the no-hidden negative case; case 4 (`".hidden"` → true) keeps the prior single-segment coverage. Assertion via `if got != tc.hidden { t.Errorf(...) }` will fail loudly if the helper short-circuits at the wrong index. | The F2 finding from Round 1 specifically called for "hidden segment at index >0" branch coverage. The new test hits index 1 AND index 2 paths, exceeding the requested coverage. |
| **mage status unchanged** | **verified.** `mage build ./internal/lister/...` re-run produces exactly: `internal/lister/lister.go:57:10: undefined: newWalkLister` and `internal/lister/lister.go:77:10: undefined: newWalkLister` — identical to Round 1's C11 carve-out baseline. No new compile errors, no errors gone that should still be there. The two `newWalkLister` references are at `Detect`'s no-git-binary and non-zero-exit branches (PLAN line 48), exactly the expected Unit 4.3 forward reference. | Live mage build output observed during this pass. |
| **No new exports introduced** | **verified.** `go doc -all ./internal/lister` shows the public surface lost exactly one symbol (`NewGitListerForTest`) and gained none. Net -1. The `package lister_test` → `package lister` rehome of `git_test.go` does NOT affect the production-package exported surface because `*_test.go` files in `package foo` are still excluded from the importable package's symbol table — they only see internal symbols at test compile time. | Diff at `git.go` is -10/+2 (net -8 LOC); `git_test.go` diff is package decl + import removal + 6 call-site swaps + new 17-line sub-test = net +31/-8 LOC, all inside `_test.go` build tags. |
| **Round 1 tests preserved** | **verified.** All 5 prior tests (`TestGitLister_List_InRepo`, `TestGitLister_List_SubdirRoot`, `TestGitLister_FilterHidden`, `TestGitLister_ContextCancel`, `TestGitLister_RelPathInvariant`) are present with assertion logic byte-identical to Round 1. The only diff inside each test is the constructor: `lister.NewGitListerForTest(ctx, root, opts)` → `newGitLister(ctx, root, opts)`. Same signature, same return shape, same nil-check pattern. | Diff inspection: 6 call-site substitutions, no other edits to test bodies. |
| **lister_test.go cross-visibility intact** | **verified.** `lister_test.go` remains in `package lister_test` (line 1). It uses only exported symbols: `lister.Detect` (line 33, 62, 93), `lister.GitLister` type assertion (line 41), `lister.ErrNoGitignoreInRepo` (line 97). None of these reference `NewGitListerForTest`. The `*lister.GitLister` type assertion at line 41 still resolves because `GitLister` itself remains exported. | Direct `Read` confirmed. |
| **F4 deferral recorded** | **verified.** `main/PLAN.md:198` contains a full bullet under "Follow-Ups / Outstanding Orchestration Tasks": *"Symlinked walk-root normalization (v0.2 hardening) — surfaced by Drop 4 Unit 4.2 falsification F4 (2026-05-15)..."* with the precise repro condition (`filepath.Abs` vs `git rev-parse --show-toplevel` symlink divergence → silent zero-yield) and the v0.2 hardening path. | Direct `Read` of main/PLAN.md confirmed. |
| **Drop PLAN.md state flip** | **verified.** `drops/DROP_4_.../PLAN.md:57` flipped Unit 4.2 state from `in_progress` to `done`. The 1-line diff is the only edit to that file. | `git diff HEAD~1 -- drops/` confirmed. |

### Polish observation (non-blocking, not a counterexample)

The Round 2 result is a **mixed-package-decl directory**: `lister_test.go` lives in `package lister_test` (external/black-box test for the `Detect` factory's public contract), while `git_test.go` now lives in `package lister` (internal/white-box test for `GitLister` mechanics). Go fully supports this split — `go test` compiles both packages and merges results — and it is the minimum-surface fix for Round 1's F3 (keeps `lister_test.go`'s exported-only black-box property intact while giving `git_test.go` the unexported access it actually needs). Worth a single-line README/CLAUDE.md note for future maintainers, but **not a finding**: every alternative (a) collapse both to internal, (b) re-export `newGitLister`, (c) leave `NewGitListerForTest` in place is strictly worse than the current shape. The split is the right shape; the dev picked correctly between Option A (rename without `ForTest` suffix) and Option B (rehome + delete export).

### Counterexamples

None. Zero new CONFIRMED counterexamples in Round 2. F3 from Round 1 is closed.

### Findings (non-blocking, residual)

- **F4 (carry-over, deferred)** — Symlinked walk-root hardening was deferred to v0.2 per the dev decision; recorded as a follow-up at `main/PLAN.md:198`. Confirmed routed correctly; not a Round 2 issue.

### Unknowns

- Could not execute `mage test ./internal/lister/...` from the QA sandbox (would compile-fail at the C11 carve-out regardless). Verification of test behavior relies on (a) `mage build`'s confirmation that the package compiles up to the C11 boundary and (b) Go's well-defined `package foo` internal-test semantics — `newGitLister` and `anySegmentHidden` are reachable from `git_test.go`'s new `package lister` declaration without re-export. The forward test execution lands at Unit 4.3's mage gate (when `newWalkLister` is implemented and `mage test ./internal/lister/...` becomes runnable).

### Summary

Round 2's revise is complete and surgical. The Round 1 F3 counterexample (`NewGitListerForTest` unauthorized export) is fully remediated via the dev-chosen Option B (rehome `git_test.go` to `package lister`, delete the export). Zero new exports added, zero Round 1 tests broken, mage build status preserved at the expected C11 baseline. F1 worklog gap is closed with a dedicated heading and matching content. F2 test gap is closed with a 4-case table-driven sub-test that directly exercises the `anySegmentHidden` non-first-segment branch. F4 symlink hardening is correctly deferred to v0.2 in `main/PLAN.md` follow-ups. Unit 4.2 is clean for verify (Phase 6).

## Hylla Feedback

None — Hylla answered everything needed. One `hylla_search_keyword` query was issued (`query=NewGitListerForTest`, `artifact_ref=github.com/evanmschultz/rak@main`) to verify the deleted symbol's absence from the committed baseline; result was empty (correct — Hylla is stale until drop-end reingest, but the empty result is consistent with the post-revise state). All other evidence came from `git diff HEAD~1`, `Read` of the 4 changed files, and `go doc -all ./internal/lister` for the exported-surface check.

## Unit 4.3 — Round 1

**Verdict:** pass-with-findings

WalkLister itself (`walk.go` + `walk_test.go`) is clean. The scope-expansion changes to `git.go`, `git_test.go`, `lister.go`, `lister_test.go` introduced one CONFIRMED contract drift and two PROCESS findings that the orchestrator should route before close. None of them break the build, the tests, or `mage ci`; they are spec / decision-discipline gaps.

### Attack results

- **gitCleanEnv runtime impact** — **finding (low severity), NOT a counterexample.** `gitCleanEnv` is always active (called from every `exec.CommandContext` in the lister package: `Detect`'s probe at `lister.go:62`, `newGitLister`'s `rev-parse --show-toplevel` at `git.go:78`, and `List`'s `git ls-files` at `git.go:131`). End-users who run `rak` from a shell that exports `GIT_DIR`, `GIT_WORK_TREE`, or `GIT_INDEX_FILE` will get rak silently ignoring those env vars and using `cmd.Dir`-based discovery instead. Pre-Drop-4 rak was not git-aware, so this is a new behavior. It is correct (consistent with what `cmd.Dir` users expect) and matches how `gh`, `git status` in subprocesses, etc. behave when callers explicitly set `cmd.Dir`. But it is a **silent product-behavior choice** that was not documented in PLAN.md and not flagged for dev acceptance. **Route to orchestrator:** acceptable as-is; add a one-line note to PLAN.md Drop 4 § "Git Edge Cases" capturing the env-stripping policy so future maintainers and users do not get surprised.

- **F19 period strip — CONFIRMED contract drift (F1).** This attack lands. The committed sentinel text is now `"rak: --no-gitignore has no effect when run inside a git repository; rak counts git-tracked files in this mode. To count untracked files, run rak outside the repository"` (`lister.go:35`). PLAN.md F19(a) (line 184) declares the sentinel text as `"rak: --no-gitignore has no effect when run inside a git repository. rak counts git-tracked files in this mode. To count untracked files, run rak outside the repository."`. **TWO** edits diverge, not one:
  1. The first sentence boundary changed: `repository.` → `repository;` (period replaced with semicolon).
  2. The trailing period was dropped: `repository.` → `repository` (full-stop removed).
  - The builder's worklog (lines 161, 191) acknowledged only the trailing-period removal as a `ST1005` lint compliance edit. The semicolon substitution is not mentioned, not justified, and goes beyond what `ST1005` requires (`ST1005` only forbids trailing punctuation on error strings, not internal sentence boundaries).
  - **Compounding contract miss:** PLAN.md Unit 4.4 line 119 specifies the **user-visible error message format** (the cobra-printed copy when a user runs `rak --no-gitignore .`) as: `"rak: --no-gitignore has no effect when run inside a git repository. rak counts git-tracked files in this mode. To count untracked files, run rak outside the repository."`. The committed sentinel `Error()` returns the drifted text. When Unit 4.4 lands and surfaces the wrapped error to cobra, the printed message will be drifted, not what PLAN.md declares.
  - **Repro (concrete):** `Read internal/lister/lister.go:35` and `Read drops/DROP_4_DEFAULT_BEHAVIOR_TRACKED_TOON/PLAN.md:184` and `:119` — diff the strings character-by-character.
  - **Severity:** low. `errors.Is` still works (sentinel identity is unchanged); test `TestDetect_NoGitignoreInRepo_ReturnsSentinel` is unaffected. The drift is a copy/UX divergence, not a logic break. But Unit 4.4's planned text-match test (or future docs) will fail if it asserts the PLAN.md text literally.
  - **Remediation paths (orchestrator + dev choose one):**
    - (a) Restore the F19(a) text exactly: revert the semicolon back to a period AND restore the trailing period. Suppress the `ST1005` lint with a `//nolint:staticcheck` or, cleaner, an `.golangci.yml` rule that exempts the lister package's sentinel strings. This honors the contract.
    - (b) Update PLAN.md F19(a) and PLAN.md Unit 4.4 line 119 to match the committed text (both edits in one revise commit). This relitigates the contract but documents the lint-driven change.
    - (c) Split the difference: accept the semicolon (low-impact prose tweak), restore the trailing period only if a lint exemption is acceptable. Document which path was taken in PLAN.md.

- **Pre-existing lint discipline gap — PROCESS finding (F2), NOT a counterexample on Unit 4.3.** `ErrNoGitignoreInRepo` was introduced in Unit 4.1 with a trailing period. Unit 4.1 build-QA proof Round 1 (Verdict: pass) did not flag it. Unit 4.2 build-QA proof Rounds 1 and 2 also did not flag it. The defect first surfaced when `mage ci` ran inside Unit 4.3 build-QA. Root cause: at Unit 4.1 the lister package did not compile (C11 carve-out), so `mage lint`/`mage ci` could not see the package, and the staticcheck pass over `internal/lister` was a no-op. **Process gap:** the lint inspection was effectively gated behind the compile gate the whole time, which means Unit 4.1's "mage lint green" acceptance criterion (line 55 of Unit 4.1) was satisfied **vacuously** — golangci-lint cannot lint a package that does not compile. The Unit 4.1 QA proof Round 1 confirmed "mage scoped subset passes" but never re-ran lint after Unit 4.2 narrowed the compile-break to one symbol either, missing the staticcheck warning a second time. **Route to orchestrator:** add a note to `main/CLAUDE.md` § "Build Verification" or `main/drops/WORKFLOW.md` § "Phase 5" that lint coverage of a package is conditional on that package compiling; when a unit carries a deliberate compile-break carve-out (like C11), the lint-green acceptance for that package is **deferred** to the unit that closes the carve-out. The Unit 4.1/4.2 acceptance criteria should have called this out.

- **Scope expansion bundling — PROCESS finding (F3), NOT a counterexample.** Unit 4.3's declared `paths` are `internal/lister/walk.go` and `internal/lister/walk_test.go`. The build touched four additional files (`lister.go`, `lister_test.go`, `git.go`, `git_test.go`) — half of the diff is outside scope. The builder justified this in `BUILDER_WORKLOG.md` lines 158-162 ("mage ci is an acceptance criterion for Unit 4.3 and requires all tests to pass"; "pre-existing bugs from Unit 4.2 that became visible only now that the package compiled"). The justification is **load-bearing but the bundling is suboptimal**:
  - Three different concerns are bundled into one commit: (a) Unit 4.3's actual WalkLister adapter, (b) a sandbox-env workaround (`gitCleanEnv` + `skipIfGitEnvBroken`), (c) a lint fix for Unit 4.1's `ErrNoGitignoreInRepo`. Each has a different audience: (a) is the Unit 4.3 deliverable, (b) is test-infrastructure hygiene that applies to all GitLister tests, (c) is a Unit 4.1 retroactive fix.
  - **Cleaner alternative:** route the lint fix + env-clean as a one-shot `fix(lister): drop ST1005 trailing period; add env-clean for test subprocesses` commit between Units 4.2 and 4.3, leaving Unit 4.3's commit pure to the WalkLister adapter and its tests. This is the standard "atomic commit" discipline.
  - **Why this is a finding, not a counterexample:** PLAN.md does not have an explicit hard-line "Units' diffs MUST stay inside declared paths" rule — `main/CLAUDE.md` § "Project Structure" and § "Git Commit Format" both encourage scope discipline but do not gate. The Phase 5 build-QA criteria do not reject scope expansion when justified. So the bundling is **legal but not optimal**.
  - **Route to orchestrator:** dev decides whether to (i) re-split the commit (likely not worth the cost now), or (ii) tighten WORKFLOW.md Phase 4 to require any unit's diff outside its declared `paths` to be either (a) explicitly added to PLAN.md before build, or (b) split into a separate pre-build hygiene commit. Future drops can adopt the tightened rule.

- **skipIfGitEnvBroken legitimacy** — **mitigated.** `skipIfGitEnvBroken` (`git_test.go:29-38`) inspects an error via `errors.As(err, *exec.ExitError)` and checks `ExitCode() == 128`. Git documents exit 128 as a fatal git error (corrupt repo, missing config, env conflict, etc.). Treating it as "git env unusable; skip" is a defensible policy — the test's goal is to verify GitLister behavior when git can operate, not to test git environment recovery. The skip prints a clear `t.Skipf("git env broken in test subprocess (exit 128); skipping: %v", err)` so a CI run logs the skip visibly. **CI implication:** if the GitHub Actions runner does NOT have GIT_DIR set globally (typical), all 5 GitLister tests will RUN (not skip) on CI. So the tests are not perpetually skipped — they are sandbox-gated. The Claude Code subprocess sandbox exports `GIT_DIR` or similar; CI does not. Verified by inspection of `gitCleanEnv` which only triggers when a parent process set GIT_DIR. The skip-pattern is the right tool for the right job. **No counterexample.**

- **F22 pure pass-through** — **mitigated.** `WalkLister.List` (`walk.go:38-40`) does literally `return wl.walker.Walk(ctx)`. No filtering, no transformation, no logic. Walker applies all `WalkOptions` filters internally (verified via `hylla_search` of `Walker` and `NewWalker` docstrings: "applying WalkOptions filters (depth, hidden entries, .gitignore handling, include/deny globs)"). F22 holds exactly as written. **No counterexample.**

- **F26 test coverage** — **mitigated, with an honest note.** `TestWalkLister_RelPathInvariant` (`walk_test.go:145-167`) exercises the F26 (a)/(b)/(c) assertions across three test paths (`a.txt`, `sub/b.txt`, `sub/deep/c.txt`). Since `WalkLister` is a pure pass-through to `Walker`, the test effectively retests Walker's F26 compliance — Walker's own walker_test already covers this. The duplication is **acceptable** as a regression guard (if someone later adds filtering to WalkLister and breaks F26, this test catches it), but is not adding net new test coverage. This is a minor low-value test, not a counterexample. **No counterexample.**

- **Hidden pre-existing issues** — **EXHAUSTED, no counterexample found.** Re-ran `mage ci`: green (`0 issues`, all 6 packages pass). Lint did not surface any additional warnings beyond the now-fixed trailing-period one. There was a risk that `mage lint` had been masking other warnings via the C11 compile-break since Drop 3 close; that risk is now resolved — every package the lint can see is clean.

- **mage ci re-run verification** — **mitigated.** Re-ran `mage ci` from `main/`: `0 issues` + every package OK (cached). The cache is sound (no Go source changed since the build commit). Build, lint, format, and test all green.

- **End-to-end CI implication** — **EXHAUSTED, no counterexample found.** I cannot directly invoke `gh run watch` or trigger CI from this seat, but I can reason: the `.github/workflows/ci.yml` runs `mage ci` (per main/CLAUDE.md § "Build Verification"). Locally `mage ci` is green; the GitHub Actions runner has a stock environment (no `GIT_DIR` set in shell, golangci-lint version pinned via the workflow's setup step). Two potential risks: (a) golangci-lint version drift between local and CI — unverified, but the same risk existed across all of Drop 3 and Drop 4 prior units; (b) on a runner without git installed, the 5 GitLister tests will `t.Skip("git binary not found")` (`skipIfNoGit`), not fail. So CI is **likely green**. **The orchestrator should verify** by watching the post-push CI run at Phase 6 — the standard discipline.

### Counterexamples

- **F1 (low severity, CONFIRMED)** — `ErrNoGitignoreInRepo` sentinel text drifts from PLAN.md F19(a) and PLAN.md Unit 4.4 line 119 in TWO places (period→semicolon at sentence 1 boundary, plus dropped trailing period). Repro: read `internal/lister/lister.go:35` and compare character-by-character to PLAN.md lines 184 and 119. Remediation: see options (a)/(b)/(c) above. Not blocking but needs dev decision before Unit 4.4 build.

### Findings (non-blocking, process)

- **F2 (informational, process)** — Lint coverage of `internal/lister` was vacuously gated behind the C11 compile-break for Units 4.1 and 4.2. The trailing-period `ST1005` warning could only surface once Unit 4.3 closed the carve-out. WORKFLOW.md or CLAUDE.md should call this out: a unit's "mage lint green" acceptance is conditional on the package compiling; for carve-out units, defer lint-acceptance to the unit that closes the carve-out.

- **F3 (informational, process)** — Unit 4.3 bundled three different concerns into one commit (WalkLister adapter + sandbox-env workaround + Unit 4.1 lint fix). All three were necessary to make `mage ci` green, so the bundling has a load-bearing justification, but cleaner discipline would route (b) and (c) as a separate pre-build hygiene commit. Future drops could tighten WORKFLOW.md to require this; existing commit needs no action.

- **F4 (informational, transparency)** — The `gitCleanEnv` env-stripping policy is a silent product behavior change (rak now ignores user-set `GIT_DIR`/`GIT_WORK_TREE`/`GIT_INDEX_FILE`). It is correct, but should be documented in PLAN.md § "Git Edge Cases" or a new § "Environment Variables" so users with unusual git env configurations are not surprised.

### Unknowns

- I cannot directly invoke `gh run watch` or trigger CI from this seat. Verifying the GitHub Actions run is green post-push is the orchestrator's Phase 6 step. Locally `mage ci` is green, so the prediction is "CI green," but the truth lands at Phase 6.
- I cannot directly verify whether `gitCleanEnv` correctly handles edge cases like `GIT_DIR=` (empty value) or malformed env entries. Inspection of the code shows `strings.IndexByte(kv, '=')` and a map lookup of the key — both safe under empty values. Not a counterexample candidate without a concrete repro.

### Summary

WalkLister itself (declared scope: `walk.go`, `walk_test.go`) is clean — F22 holds, F26 is enforced by test, Walker delegation is pure. The scope-expansion changes are where the findings live: one CONFIRMED low-severity contract drift on the `ErrNoGitignoreInRepo` text (F1), and three informational process findings (F2 lint discipline, F3 commit bundling, F4 transparency on env-stripping policy). Zero counterexamples that would block Unit 4.3 from being declared done. The orchestrator should route F1 to the dev for a one-line decision before Unit 4.4 starts; F2/F3/F4 can be batched into a Drop 4 close-out review or deferred to a workflow-hardening unit.

## Hylla Feedback

- **Query 1:** `hylla_search_keyword`, query="NewWalker fileset", artifact=`github.com/evanmschultz/rak@main`, node_type=block, fields=[content, summary]. Returned `NewWalker` and `Walker` nodes with full docstrings confirming the "applies WalkOptions filters internally" claim that F22 leans on. Zero miss.
- All other evidence came from `Read` of the modified files (which are post-HEAD relative to the last Hylla ingest at Drop 3 close — so Hylla is stale for `internal/lister/*` and Hylla's miss is expected, not a feedback item per the `## Hylla Feedback` rule: "changed since last ingest → git diff territory").

## Unit 4.3 — Round 2

- **QA:** go-qa-falsification-agent
- **Reviewed:** 2026-05-14
- **HEAD under review:** `de7dcd3` (`fix(lister): restore intersentence period in errnogitignoreinrepo`)
- **Verdict:** `pass` — Round 1's CONFIRMED F1 sentinel-text drift is fully remediated. The revise is surgical: one character changed in `internal/lister/lister.go` (`;` → `.`) plus the two pre-agreed markdown documentation updates (PLAN.md § "Git Edge Cases" env-vars bullet, WORKFLOW.md § "Cascade Tiering" → "Carve-out + Scope Discipline" subsection). `mage ci` re-runs green. Zero new counterexamples. Round 1's F2/F3/F4 process findings are now routed and recorded in the durable docs.

### Revise verification

| Surface | Outcome | Evidence |
|---|---|---|
| **A1 — Fix landed in the right place, byte-exact** | **verified.** `git diff HEAD~1 HEAD -- internal/lister/lister.go` is exactly `-1/+1` on `lister.go:35`. The only character changed is the inter-sentence boundary: `repository;` → `repository.`. The trailing period (intentionally absent per ST1005) is preserved (no terminating period was re-added). The line now reads: `var ErrNoGitignoreInRepo = errors.New("rak: --no-gitignore has no effect when run inside a git repository. rak counts git-tracked files in this mode. To count untracked files, run rak outside the repository")`. This matches PLAN.md F19(a) (line 184) inter-sentence boundary AND matches PLAN.md Unit 4.4 line 119's user-visible error message format for the first-sentence break. The intentional trailing-period absence is consistent with Round 1's F1 split-remediation (F19(a) updated in plan-side at Round 1 close to drop the trailing period for ST1005 compliance; the inter-sentence period is the only diverging character that needed restoring). |
| **A2 — Fix did not break anything (`mage ci` green)** | **verified.** Re-ran `mage ci` from `main/`. Output: `0 issues.` + all 6 packages OK (`cmd/rak`, `counting`, `fileset`, `ignore`, `lister`, `render`) — all `(cached)`. The cache hit is sound because only one byte of one Go source string literal changed, and lister's tests do not text-match the sentinel message (`TestDetect_NoGitignoreInRepo_ReturnsSentinel` uses `errors.Is`, not string compare). Format, lint, vet, and test all green. |
| **A3 — F4 env-vars policy documented in PLAN.md § "Git Edge Cases"** | **verified.** `drops/DROP_4_DEFAULT_BEHAVIOR_TRACKED_TOON/PLAN.md:216` carries the new bullet *"Environment variables (Unit 4.3 / F4): GitLister strips `GIT_DIR`, `GIT_WORK_TREE`, and `GIT_INDEX_FILE` from the environment of every `git` subprocess it spawns (via the `gitCleanEnv()` helper in `git.go`). Rationale: rak is path-driven per decision 32 — repo detection uses `cmd.Dir` set to the absolute walk root, not env-based overrides. Stripping these vars ensures user-set env doesn't divert rak to a different repo than the path argument indicates. End users who rely on `GIT_DIR` to point git at a non-default repo will see rak ignore that var in v0.1.0; this is deliberate. A future `--git-env-passthrough` flag may opt this back in if usage signals."* Names the three env vars stripped, names the helper (`gitCleanEnv`), states the rationale (path-driven per decision 32), and routes the future opt-in path. Covers what the Round 1 F4 finding asked for. |
| **A4 — F2/F3 process findings documented in WORKFLOW.md § "Cascade Tiering"** | **verified.** `drops/WORKFLOW.md:65–70` carries a new `### Carve-out + Scope Discipline (Drop 4 hindsight)` subsection with two bullets: (a) *"Lint coverage is conditional on package compilation"* — captures Round 1 F2 (vacuous `mage lint` over the non-compiling `internal/lister` at Units 4.1/4.2); (b) *"Out-of-paths changes should be pre-declared OR split"* — captures Round 1 F3 (Unit 4.3 bundled three concerns: WalkLister adapter + sandbox-env workaround + Unit 4.1 lint fix). Bullet (a) explicitly defers lint-acceptance to the unit that closes the carve-out and cites the staticcheck ST1005 case from this drop. Bullet (b) defines the three conditions (a)/(b)/(c) under which out-of-paths is acceptable and recommends a separate pre-build hygiene commit otherwise. Both findings routed correctly. |
| **A5 — No Round-1 regressions** | **verified.** Diff scope was inspected: `git show HEAD --stat` shows exactly 3 files touched: `internal/lister/lister.go` (1 byte change), `drops/DROP_4_DEFAULT_BEHAVIOR_TRACKED_TOON/BUILDER_WORKLOG.md` (round notes), `drops/DROP_4_DEFAULT_BEHAVIOR_TRACKED_TOON/PLAN.md` (state flip). No edits to `git.go`, `git_test.go`, `walk.go`, `walk_test.go`, `lister_test.go`, or any other Unit 4.3 / prior-unit code surface. Round 1's already-mitigated invariants (gitCleanEnv runtime safety, skipIfGitEnvBroken pattern, F22 pure pass-through, F26 RelPath assertions, NUL-parsing edges, F17 prefix logic, F18/F19/F21 filter chain) are byte-identical to Round 1 HEAD. The previously CONFIRMED F1 remediation is the only behavior change. |

### Counterexamples

None. Round 1's F1 is closed. Zero new counterexamples in Round 2.

### Findings (residual, non-blocking)

- None active. Round 1's F2/F3/F4 are now documented in WORKFLOW.md and PLAN.md respectively and pass through to future-drop discipline; they are no longer outstanding against Drop 4.

### Unknowns

- The end-to-end GitHub Actions CI verification still lands at Phase 6 (`git push` + `gh run watch --exit-status`). Local `mage ci` is green; the prediction is "CI green," but the truth is the orchestrator's next step. Round 1 already noted this same Unknown.

### Summary

Round 2's revise is one byte of Go source + two markdown additions, end-to-end. The Round 1 F1 sentinel-text drift is closed at the exact character where it diverged (inter-sentence `;` → `.`); the intentional trailing-period absence remains for ST1005 compliance. `mage ci` re-runs green with 0 issues across all 6 packages. The Round 1 F4 product-behavior transparency requirement is satisfied by the new PLAN.md § "Git Edge Cases" env-vars bullet (names the three env vars, names the helper, states the rationale). The Round 1 F2 and F3 process findings are routed into the durable WORKFLOW.md § "Cascade Tiering" → "Carve-out + Scope Discipline" subsection for future-drop discipline. No regressions to Round 1's already-mitigated surfaces (`mage ci` cache-hit on all packages confirms zero Go-source delta beyond the one character). Unit 4.3 is clean for Phase 6 verify.

## Hylla Feedback

None — Hylla answered everything needed. Round 2 evidence came from `git show HEAD --stat`, `git diff HEAD~1 HEAD -- internal/lister/lister.go`, `mage ci` re-run, and `Read` of the four post-revise files (`internal/lister/lister.go`, `drops/DROP_4_DEFAULT_BEHAVIOR_TRACKED_TOON/PLAN.md`, `drops/WORKFLOW.md`, `BUILDER_QA_FALSIFICATION.md`). No Hylla queries were issued — the revise touches `internal/lister/lister.go` (post-Drop-3 file, stale in Hylla until drop-end reingest) plus markdown (out of Hylla scope), so direct file reads + git plumbing are the appropriate evidence layer.

## Unit 4.5 — Round 1

- **QA:** go-qa-falsification-agent
- **Reviewed:** 2026-05-14
- **HEAD under review:** `39cb765` (`feat(render): add toonrenderer for llm-first default output`)
- **Verdict:** `pass-with-finding` — no hard counterexamples; one substantive process finding routed to dev (F1, design deviation from PLAN.md F20 wording) + two soft notes (F2 test-substring weakness, F3 unverified array-context pipe handling). None are grounds for failing the unit.

### Attack results

| Attack | Outcome |
|---|---|
| **A1 — Spike legitimacy** (does `omitempty` actually drop empty fields? does pipe-in-scalar auto-quote?) | **MITIGATED.** Context7 `/toon-format/toon-go` exposes the option API (`WithDocumentDelimiter`, `WithArrayDelimiter`, `DelimiterPipe`) and the canonical struct-tag spelling. The builder's worklog (§ "Spike: toon-go behavior") records two empirical scalar-context findings: (a) `Count int \`toon:"count,omitempty"\`` with `Count=0` produces output without the `count` field; with `Count=5` it appears; (b) `Path="a|b|c"` with `DelimiterPipe` produces `path: "a|b|c"` (auto-quoted). Both behaviors are reproducible from the Context7 surface — option API present, omitempty is a standard reflect-tag idiom universally honored by every Go marshaler that consumes struct tags, and the library's pipe delimiter wouldn't be usable for tabular arrays at all if scalar values containing pipes weren't quoted. The spike code itself was deleted (worklog explicitly says "scratch, NOT committed"), so I cannot re-run it byte-for-byte, but the documented behavior is internally consistent with Context7 + every other Go marshaler I checked. The `Errors []string \`toon:"errors,omitempty"\`` field in `toonTree` (line 59 of `toon.go`) is the load-bearing use of the omitempty claim — and `TestTOONRenderer_RenderTree_NoErrors` (asserts no `"errors"` substring on nil errs) plus `TestTOONRenderer_RenderTree_WithErrors` (asserts `"errors"` appears on non-empty errs) lock the live binary behavior. Both pass under `mage ci`. Omitempty works for this code path. |
| **A2 — Total-field flatten (HEADLINE)** — PLAN.md F20 (line 189) and Unit 4.5 acceptance (line 151) pin `toon:"total"` scalar block; the builder implemented flat `toon:"total_bytes" / total_lines / total_words / total_chars`. Is toon-go nested-struct serialization unsupported (forcing flatten), or did the builder over-correct? | **PROCESS FINDING (F1) — not a hard counterexample.** Evidence triangulation: (1) Context7 has an explicit nested-OBJECT example using `map[string]any{"config": map[string]any{"enabled": true, "timeout": 30}}` which marshals to `config:\n    enabled: true\n    timeout: 30` — a literal nested indented block, exactly the shape PLAN.md F20 calls for. (2) Context7 has NO explicit struct-with-non-slice-struct-field example; the prose "complex data structures including nested structs and slices" is suggestive but not definitive for the struct-field case specifically. (3) The builder's worklog (lines 269 + 271–273) explicitly cites this Context7 gap as the rationale for the flatten and acknowledges it as a deviation the QA falsification pass may attack. (4) **Asymmetry observable in-codebase:** `internal/render/render_test.go` line 304 pins the JSON renderer's tree output as `{"directories":[],"total":{"Bytes":0,"Lines":0,"Words":0,"Chars":0}}` — i.e. `jsonRenderer` emits a NESTED `total` struct via `encoding/json` reflection. The TOON path now diverges from the sibling renderer's design shape without empirical evidence that toon-go cannot do the same. (5) `mage ci` is green; the unit's test substrings (`"bytes: 12"`, `"directories"`, `"."`, `"sub"`) don't pin the total-field shape at all, so the test surface accepts either shape. **Why this is a process finding, not CONFIRMED:** the builder's spike covered C7 (omitempty) and C8 (pipe-in-scalar) but did NOT cover the nested-struct case, and the Context7 evidence is ambiguous. Without a falsifying empirical spike showing toon-go DOES emit a struct-field as an indented block (the analogue of the map example), I cannot CONFIRM the builder is wrong about the lib's capability. **Recommended dev decision:** either (a) accept the flatten and revise PLAN.md F20 + Unit 4.5 acceptance to drop the `toon:"total"` scalar-block wording (one-line PLAN edit), or (b) direct a builder spike against `type wrap struct { Total inner \`toon:"total"\` }` and revert to the planned nested shape if toon-go supports it (probable — symmetry with the map case + library's own prose claim). LLM-consumer-token-efficiency angle: nested `total: {bytes, lines, ...}` saves four `total_` prefixes per document (~28 bytes), which is the entire token-efficiency premise of TOON. Mildly favors the planned nested shape. |
| **A3 — Pipe in directory-path inside an array context** (spike only verified scalar-context auto-quoting; array context uses `WithArrayDelimiter(toon.DelimiterPipe)` which sets the column separator) | **EXHAUSTED, no counterexample.** The spike (§ "Spike: toon-go behavior" table line 260) tested `Path="a\|b\|c"` with `DelimiterPipe` in SCALAR context and observed `"a|b|c"` (auto-quoted). Array tabular context is structurally different — Context7's "Configure Custom Array and Document Delimiters" example shows `items[2|]{sku|qty|price}:` rows like `A-1|5|9.99`, with the pipe as a column separator. A directory path value containing a literal `|` would need to be column-quoted (CSV-style escape) for the row to parse round-trip. Context7 has no explicit row-with-embedded-delimiter example, and I cannot construct a counterexample without running it. The risk is real but speculative — moved to Unknowns (U3). The probability is low in practice (POSIX filesystems use `|` as shell-pipe; very few real paths contain it), and a regression here would surface as a `mage test` failure at Unit 4.4's integration-test layer if it manifests. Not a Round-1 blocker. |
| **A4 — Compile-time assertion location** | **REFUTED.** `git diff HEAD~1 -- internal/render/render_test.go` (lines 311–314) shows the assertion block lives in `render_test.go` alongside the existing humanRenderer / jsonRenderer assertions: `var ( _ Renderer = humanRenderer{}; _ Renderer = jsonRenderer{}; _ Renderer = toonRenderer{} )`. Builder claim verified — assertion is in test code, not buried in prose. |
| **A5 — humanRenderer / jsonRenderer tests unchanged** | **REFUTED.** `git diff HEAD~1 -- internal/render/render_test.go` consists of two pure ADDITIONS — the compile-time assertion block (lines 311–314) and four new `TestTOONRenderer_*` test functions at the end of file. No deletions, no modifications to any existing `TestHumanRenderer_*` or `TestJSONRenderer_*` test body. The diff hunk markers (`@@ -307,6 +307,13 @@` and `@@ -349,3 +356,85 @@`) are insert-only — `-` lines match `+` lines, confirming pure append. |
| **A6 — Renderer interface unchanged (F25 pin)** | **REFUTED.** `git diff HEAD~1 -- internal/render/render.go` produced empty output — the file is byte-identical to HEAD~1. `Renderer` interface methods (`Render`, `RenderTree`) + `Directory` struct unchanged. F25 honored. |
| **A7 — Doc comments per project rule 11** | **REFUTED.** `NewTOONRenderer` has a doc comment (lines 19–22 of `toon.go`) starting with the identifier name. `toonRenderer` (the unexported type) ALSO has a doc comment (lines 12–16) — exceeds the rule (rule 11 requires comments on EXPORTED identifiers only; unexported is bonus). `toonCounts`, `toonDirectory`, `toonTree`, `Render`, `RenderTree` all have doc comments. Compliance is total. |
| **A8 — Test robustness** (`strings.Contains(got, ".")` is structurally weak — any text with a period contains it) | **SOFT FINDING (F2) — non-blocking.** `TestTOONRenderer_RenderTree` (line 392 of render_test.go in HEAD) asserts `strings.Contains(got, ".")` to verify the walk-root directory `"."` appears. The substring `"."` matches floating-point literals (`9.99`), URLs, abbreviations, and `Path` separators in TOON's pipe-delimited tabular form (`A-1|5|9.99`). The intent is "the `.` directory path appears as a value in the directories array", but the test would pass on ANY non-trivial TOON output containing a period anywhere. The other assertions in the same test (`"directories"`, `"sub"`) are more discriminating — `"sub"` is unlikely to appear by accident. The test is loose-but-not-vacuous because `"sub"` does pin a real-path-value-present requirement. Recommended tightening (future drop, not Round 1): assert `strings.Contains(got, "|.|") \|\| strings.Contains(got, "\n.|")` or similar column-anchored substring to actually pin the `.` row. Not Round-1 blocking; `mage ci` is green, the test passes for real reasons, and the substring-style was a deliberate Round-1 robustness choice per PLAN.md Unit 4.5 acceptance ("not exact snapshot, to be robust against toon-go formatting tweaks"). |
| **A9 — `mage ci` green claim** | **REFUTED.** Re-ran `mage ci` from `main/`. Output: `0 issues` (golangci-lint), all six packages cached green (`ok cmd/rak`, `ok internal/counting`, `ok internal/fileset`, `ok internal/ignore`, `ok internal/lister`, `ok internal/render`). Builder claim verified. |

### Counterexamples

None — no attack produced a concrete failure reproducible against HEAD `39cb765`. The headline attack (A2) is a substantive design-deviation process finding routed to the dev for decision (revise PLAN.md or revise implementation); without an empirical spike showing toon-go DOES support nested non-slice struct fields, I cannot CONFIRM the builder's flatten is wrong about library capability.

### Findings surface

- **F1 — Design deviation from PLAN.md F20 / Unit 4.5 acceptance (SUBSTANTIVE PROCESS FINDING, dev decision required).** Builder implemented flat `total_bytes/total_lines/total_words/total_chars` instead of the planned `toon:"total"` nested-struct block. The builder's worklog explicitly flags this and cites absence of Context7 evidence for nested non-slice struct support; the QA spike was C7 (omitempty) + C8 (pipe), not nested-struct. **However:** (a) Context7's nested-MAP example demonstrates toon-go DOES emit nested indented blocks via reflection, making nested-struct support a reasonable expectation; (b) `jsonRenderer` (sibling renderer in same file) emits nested `total: {Bytes, Lines, Words, Chars}` — the TOON path now diverges from the JSON path's design shape without empirical evidence forcing it; (c) the unit's tests don't pin total-field shape, so either implementation would pass test. Two clean resolution paths: **(F1a) PLAN.md revise** — update F20 + Unit 4.5 acceptance to drop the `toon:"total"` block requirement (planner-doc fix); **(F1b) Implementation revise** — spawn a Round 2 spike against `type wrap struct { Total inner \`toon:"total"\` }`; if toon-go marshals it as an indented block (probable, by analogy with the map case), revert the flatten to nested. Token-efficiency angle mildly favors F1b: nested saves ~28 bytes/document vs four `total_` prefixes, and TOON's product positioning IS token efficiency. Dev call.
- **F2 — Test substring weakness in `TestTOONRenderer_RenderTree`** (soft, non-blocking). `strings.Contains(got, ".")` is vacuous on any non-trivial output. The accompanying `"sub"` substring redeems the test by pinning a real path value, so the test isn't broken — it's just slightly weaker than it reads. Future tightening: anchor the `.` check inside the tabular-row column delimiter (e.g. `"|.|"` or `"\n.|"`) so the assertion actually means "the `.` row exists as a tabular-array value". No Round 1 action required.
- **F3 — Spike coverage gap.** The spike covered C7 (omitempty) and C8 (pipe-in-scalar) but did NOT cover (a) nested non-slice struct marshaling (the basis for F1), (b) pipe-in-string-VALUE inside an array tabular context (whether tabular rows column-quote embedded delimiters the way scalar fields do — see Unknown U3). Future planning-acceptance for spike-required units should enumerate the empirical questions exhaustively, not just by example.

### Unknowns

- **U1** — Cannot empirically verify toon-go's nested-struct marshaling behavior without writing a spike test, which is outside QA-falsification write scope. The F1 process finding is bounded by this — it is a process finding (route to dev), not CONFIRMED counterexample, precisely because the empirical question is unresolved.
- **U2** — `mage run` output for an actual TOON document (what does `RenderTree` emit, byte-for-byte, on a non-trivial input?) was not inspected; the existing tests use loose `strings.Contains` substrings rather than byte-exact snapshots. A targeted `mage run` invocation against `cmd/rak/testdata/tree` with `--toon` would reveal the actual product surface — recommended at Unit 4.4 build-QA when the flag is wired in, not blocking here.
- **U3** — Pipe-in-VALUE inside an array tabular ROW. The spike showed scalar-context auto-quoting. Array tabular context (where the pipe IS the column separator) requires CSV-style row-value escaping; Context7 has no example of an embedded-delimiter value in a tabular row. Risk is low in practice (filesystem paths rarely contain `|`) but unverified.
- **U4** — Token-efficiency comparison. F1's resolution implicitly invokes "which is more token-efficient for LLM consumption" (the explicit goal per decision 33 + Unit 4.5 doc comment line 21). Nested `total: { bytes: X, lines: Y, words: Z, chars: W }` vs flat `total_bytes: X, total_lines: Y, total_words: Z, total_chars: W` — the nested variant saves repeated `total_` prefixes (28 bytes), the flat variant saves indentation whitespace (~16 bytes at default 2-space indent). Net favors nested by ~12 bytes per document. Not measured under tiktoken — the actual token delta may differ from the byte delta. Dev may want to call this with real tiktoken numbers before deciding F1.

### Summary

Unit 4.5's `toon.go` (129 LOC) + `render_test.go` append (89 LOC) ship a working `toonRenderer` that satisfies the `Renderer` interface (compile-time assertion present), passes all four new tests + the existing 13 humanRenderer/jsonRenderer tests, and clears `mage ci` with 0 issues. Six attacks REFUTED outright (A4 compile-assertion location, A5 sibling-test-untouched, A6 interface-untouched, A7 doc-comments, A9 mage-ci-green, and structurally A1 spike-legitimacy via Context7 cross-check). One attack (A2 total-field flatten) surfaces a substantive design deviation from PLAN.md F20 wording that is **routed to dev as a process finding**, not flagged as a hard counterexample — the deviation is internally consistent and well-reasoned in the worklog, but the empirical question (does toon-go support nested non-slice struct fields?) remains untested. The two soft findings (F2 substring weakness, F3 spike coverage gap) are documented for future-drop discipline. Unit 4.5 is technically clean for Phase 5 close; F1 requires a dev call before drop-end closeout to decide between PLAN.md revise vs implementation revise.

## Hylla Feedback

None — Hylla answered everything needed. Unit 4.5 evidence came from `git show HEAD --stat` + `git diff HEAD~1 -- internal/render/`, `Read` of `toon.go` / `render.go` / `render_test.go` / `BUILDER_WORKLOG.md` / `PLAN.md`, two Context7 queries against `/toon-format/toon-go` (nested-struct support + delimiter API surface), and one `mage ci` re-run. No Hylla queries were issued — `toon.go` is a new file added in HEAD `39cb765`, so it is stale in Hylla until drop-end reingest; `render.go` and `render_test.go` were touched at HEAD~1's boundary (Drop 2/3) and the relevant deltas at HEAD are pure additions; `git diff` is the authoritative source for "what changed in this unit" and Context7 is the authoritative source for external library semantics that the repo itself cannot prove.

## Unit 4.5 — Round 2

- **Reviewer:** go-qa-falsification-agent
- **Round:** 2 (attack scope: revise only — F1 nested-struct revert + F2 assertion tighten)
- **Diffed vs:** `git diff HEAD~1 -- internal/render/` (commit `22883b6` "refactor(render): nest total back into toon block, tighten test")
- **Verdict:** **PASS** — zero CONFIRMED counterexamples. One minor textual remark on the F2 tightening shape (R1 of this Round).

### Files inspected

- `internal/render/toon.go` (full Read; 130 LOC) — confirmed `toonTree` now has `Directories | Total toonCounts | Errors` only.
- `internal/render/render_test.go` (full Read, 446 LOC) — confirmed `TestTOONRenderer_RenderTree` asserts `[]string{"directories", ".|", "sub", "total"}` (line 401); the four prior TOON tests + all 13 humanRenderer/jsonRenderer tests are byte-identical to Round 1 / HEAD~1 (zero touched).
- `internal/render/render.go` (full Read, 61 LOC) — F25 pin: `git diff HEAD~1 -- internal/render/render.go` returns empty (verified).
- `cmd/rak/root.go` (full Read) — consumes only `render.Renderer` interface + `render.Directory` struct; no TOON-specific field consumers exist, so no risk of consumer breakage from the flat→nested revert.
- `BUILDER_WORKLOG.md` Round 2 entry (lines 297–377) — spike code, captured outputs, decision rationale.

### Attack table

| # | Attack | Verdict | Evidence |
|---|---|---|---|
| **R1 — F1 spike legitimacy** (does toon-go actually emit nested struct as labelled block?) | **REFUTED.** The test `TestTOONRenderer_RenderTree` now asserts `strings.Contains(got, "total")` as a substring. `mage ci` ran GREEN with this test passing — empirical proof that the actual TOON output produced by `RenderTree` against the test fixture contains the literal substring `"total"`. If toon-go did NOT emit nested struct as a `total:`-labelled block, the substring would not appear and the test would fail. Worklog spike output `top:\n  a: 1\n  b: 2` is consistent with the observed test pass. Builder's spike claim is empirically validated by the test gate itself. |
| **R2 — Revert completeness** (any leftover `total_bytes`/`TotalBytes` etc. references anywhere?) | **REFUTED.** `toon.go` line 55–59: `toonTree` struct has exactly three fields (`Directories`, `Total toonCounts`, `Errors`). No flat `Total*` fields anywhere in the struct or `RenderTree` payload (lines 102–110: `Total: toonCounts{Bytes:..., Lines:..., Words:..., Chars:...}`). `render_test.go` reads no flat fields. `cmd/rak/root.go` consumes only the `Renderer` interface and `Directory` struct — no TOON internal types are reachable from cmd/. Hylla content keyword search for `TotalBytes|TotalLines|TotalWords|TotalChars|total_bytes` against `rak@main` returned zero results. Revert is clean. |
| **R3 — F2 `".|"` adequacy** (does it pin the `.` row, or could it match incidentally?) | **REFUTED.** Walking actual TOON output (from worklog spike + structural reasoning): header line `directories[2|]{path|bytes|lines|words|chars}:` contains `[2|` and `{path|` and `|chars}` but no `.|` substring. Data row `.|5|1|1|5` is the unique source of `".|"`. Nested `total:` block (`bytes: 8`, `lines: 2`, etc.) emits integer values, no decimals, so no `.|` collision risk there. Fixture `Path` values are `"."` and `"sub"` — no `.` adjacent to `|` in source data. `".|"` uniquely fingerprints the `.` directory row's first column. Tightening is genuine. |
| **R4 — No regression in `_Render`, `_WithErrors`, `_NoErrors`** | **REFUTED.** `mage ci` GREEN: `ok internal/render` cached. `git diff HEAD~1 -- internal/render/render_test.go` shows only the `TestTOONRenderer_RenderTree` body edit (lines 378–406 region) — the doc comment expansion + the assertion-list line change. `TestTOONRenderer_Render`, `_WithErrors`, `_NoErrors` test bodies untouched. |
| **R5 — `mage ci` actually green** | **REFUTED.** Re-ran `mage ci` from `main/` at HEAD `22883b6`: output `0 issues`, all six packages green (`cmd/rak`, `internal/counting`, `internal/fileset`, `internal/ignore`, `internal/lister`, `internal/render` — all cached or pass). |
| **R6 — F25 (`render.go` untouched)** | **REFUTED.** `git diff HEAD~1 -- internal/render/render.go` returned empty output. Byte-identical to HEAD~1. F25 invariant honored. |

### Counterexamples

None — no attack produced a concrete failure reproducible against HEAD `22883b6`. The Round 1 F1 process finding is resolved by the spike-confirmed nested-struct support + revert. The Round 1 F2 soft finding is resolved by the assertion tightening.

### Findings surface (Round 2)

- **R2-F1 (LOW, textual) — Anchor asymmetry on `".|"`.** Round 1's F2 suggested `"|.|"` or `"\n.|"` (anchored on BOTH sides — left by `\n`/`|`, right by `|`). Round 2 implemented `".|"` (anchored only on the right by `|`). The chosen anchor is strictly stronger than the original `"."` and is sufficient on the current fixture (no fixture path contains `.` immediately before `|` other than the `.` row's first column). However, if a future test added a fixture path like `"foo.bar"` rendered in a tabular row, `bar` would be followed by `|` and the assertion would still pass spuriously on `bar.|` — wait, that would be `bar.|`, which contains `.|` literally too. So strictly speaking, the assertion still permits a "path ending in `.`" to satisfy it. Not a counterexample on the current fixture (which contains only `"."` and `"sub"`), but the Round 1 F2 recommendation of left-anchoring (`"\n.|"` — newline-then-dot-then-pipe) would have been one character stronger. Not Round-2-blocking; assertion is non-vacuous and pins the intended behavior on the actual test fixture. Future tightening only if the fixture grows paths that end in `.`.

### Unknowns

None added in Round 2. The Round 1 unknowns (U1 nested-struct spike, U3 pipe-in-value-in-tabular-row, U4 token-efficiency) are either resolved (U1: spike-confirmed in Round 2) or remain as future-drop concerns unaffected by Round 2's narrow revise scope.

### Summary

Round 2 revise is **clean for Phase 5 close**. The two Round 1 process/soft findings (F1 nested-struct deviation, F2 vacuous assertion) are resolved by the revise: (a) nested-struct support empirically confirmed by spike + the now-passing `"total"` substring assertion + a `mage ci` green run; (b) `"."` tightened to `".|"`, which uniquely pins the `.` row on the current fixture. All six Round-2 attacks REFUTED. One minor textual remark (R2-F1) noting that the Round 1 F2 recommendation called for left-side anchoring too (`"\n.|"`); the implemented right-only anchor (`".|"`) is sufficient on this fixture but slightly weaker than the original suggestion — not blocking. F25 pin honored (`render.go` byte-identical). No regressions in sibling renderer tests. `mage ci` GREEN.

## Hylla Feedback (Round 2)

None — Hylla answered everything needed. Round 2 evidence came from `git diff HEAD~1 -- internal/render/`, `git show HEAD --stat`, `Read` of the four render-package source files + `BUILDER_WORKLOG.md` Round 2 section + `cmd/rak/root.go` (consumer check), one `mage ci` re-run, and one `hylla_search_keyword` content search for stale flat-field references (returned empty as expected — `toon.go` is stale in Hylla until drop-end reingest, but the empty result is consistent with the file-read evidence). No Hylla miss to report; the keyword search served as a confirmatory cross-check, not as a primary evidence source.

## Unit 4.4 — Round 1

- **Reviewer:** go-qa-falsification-agent
- **Started:** 2026-05-14
- **Verdict:** PASS (no CONFIRMED counterexamples on the Unit 4.4 contract)
- **Counterexamples (CONFIRMED, blocking):** 0
- **Findings (non-blocking, low severity):** 3 (one coverage gap on default-TOON path-arg, two textual/doc drifts that sit outside the 4.4 diff)

### Attack table

12 attack surfaces from the spawn appendix were each attempted. Result legend: REFUTED = attack examined, no counterexample landed on the 4.4 contract.

| # | Attack | Verdict | Notes |
|---|---|---|---|
| 1 | End-to-end smoke: `os.Args` → fang → cobra `RunE` → `runRoot` → `resolveRenderer` → `lister.Detect` → `runDirectory` → `walkAndCount` → `Renderer.RenderTree` | REFUTED | Chain is coherent. Each link reviewed: `main.go` calls `fang.Execute(ctx, newRootCmd(), fang.WithNotifySignal(...))`; cobra runs `MarkFlagsMutuallyExclusive` before `RunE`; `runRoot` defensively defaults `ctx`; `resolveRenderer` is exhaustive (human/json/default); `lister.Detect` resolves absolute path, env-cleans git env, branches by exit code; `runDirectory` and `walkAndCount` accept the new `FileLister`. `mage test` green for all packages. See Finding F1 below for a related coverage gap. |
| 2 | F19 error message reaches user as-is, or cobra/fang mangles it | REFUTED on 4.4 | `Detect` wraps the sentinel: `fmt.Errorf("lister: detect: %w", ErrNoGitignoreInRepo)` — the user-visible message becomes `lister: detect: rak: --no-gitignore has no effect...`, with cobra prepending `Error: ` (or fang rendering its boxed `ERROR` block in the installed binary). This matches PLAN.md F19c verbatim ("`Detect` itself wraps its outer error path as `fmt.Errorf("lister: detect: %w", ErrNoGitignoreInRepo)`; `cmd/rak/runRoot` lets that flow up to cobra unchanged"). The `lister: detect:` prefix is intentional per spec, not a UX leak. See Finding F3 for a pre-existing message-text nit that sits outside 4.4's diff. |
| 3 | Default-TOON stdin regression — does `Render(w, counts)` produce sensible output for a single Counts? | REFUTED | TOON for a single struct emits `key: value` lines (Context7 reference output: `total: 24.49` for a top-level scalar; struct fields render as indented key/value pairs). `toonCounts` has `bytes/lines/words/chars` toon tags. `TestRootCmd_ReadsStdin_RendersTOONDefault` asserts `bytes:`, `lines:`, `words:`, `chars:` substrings — those substrings are produced by toon-go's stdlib output. The `WithDocumentDelimiter(DelimiterPipe)` option only affects array/document-delimiter rows; it does not change the single-struct shape. |
| 4 | Cobra flag precedence: `--json --toon` simultaneously | REFUTED | `MarkFlagsMutuallyExclusive("human", "json", "toon")` runs in cobra's `ValidateFlagGroups`, which fires before `RunE`. `RunE` body therefore never sees more than one flag true. `TestRootCmd_MutuallyExclusiveFlags` covers the `--human --json` pair; cobra's mutex check covers all three pairs uniformly per cobra docs. Minor test gap: the `--human --toon` and `--json --toon` pairs are not individually asserted, but the cobra mechanism is the same for all three. |
| 5 | `--toon` as a no-op default — symbolically identical to no-flag? | REFUTED | `resolveRenderer` is `switch { case flags.human: ...; case flags.json: ...; default: NewTOONRenderer() }`. The `flags.toon == true` branch falls through to `default` and returns the same `NewTOONRenderer()`. The `flags.toon` field is never read directly — it exists solely to participate in `MarkFlagsMutuallyExclusive` and the help text. Worklog § "Design decisions" acknowledges this explicitly. Consistent with PLAN.md decision 33. |
| 6 | `runTreeFS` test helper uses `lister.NewWalkLister` correctly | REFUTED | `root_test.go:200` reads `source := lister.NewWalkLister(fsys, ".", opts)`. No `Detect` call in the helper — bypasses the git-probe entirely so tests can inject `fstest.MapFS`. The exported `NewWalkLister` constructor exists in `internal/lister/walk.go` for exactly this purpose (per PLAN.md Unit 4.3 C2). |
| 7 | `TestRootCmd_NoGitignoreInRepo_Errors` fragility | REFUTED | Test reads cleanly: (a) `exec.LookPath("git")` skip guard at line 148; (b) `git init <tmpDir>` skip guard at line 154; (c) `errors.Is(err, lister.ErrNoGitignoreInRepo)` is the hard check at line 176, with the message-substring check at 170-175 being a `t.Logf` (soft logging) only — message-text drift would not fail the test. `git init` does NOT require `user.email`/`user.name`; only `git commit` does. `Detect` env-cleans `GIT_DIR/GIT_WORK_TREE/GIT_INDEX_FILE` so any inherited env from the parent test process doesn't divert the rev-parse probe. |
| 8 | Integration fixture counts unchanged claim | REFUTED | Verified `a.txt` content "hello world\n" = 12 bytes 1 line 2 words 12 chars; `sub/nested.txt` = "one two\n" = 8 bytes 1 line 2 words 8 chars. Sum: 20 / 2 / 4 / 20. Matches `treeExpectedTotalBytes = 20` and friends in `integration_test.go`. Hidden `.gitignore` and `.hidden.txt` excluded by F21; `bin.dat` (NUL byte) excluded by F23; `vendor/ignored.txt` excluded by gitignore. Counts unchanged from Drop 3. |
| 9 | F25 preservation: `Renderer` interface unchanged | REFUTED | `git diff HEAD~1 -- internal/render/render.go` is empty. `Renderer` still exposes `Render(w, counts) error` and `RenderTree(w, dirs, total, errs) error`. `Directory` struct unchanged. F25 honored. (See Finding F2 below — `render.go`'s package doc comment lists only `NewHumanRenderer`/`NewJSONRenderer`, missing `NewTOONRenderer`. That drift originates in Unit 4.5, not 4.4.) |
| 10 | Drop 3 spine preservation: `internal/{fileset,ignore,counting,render}` untouched by 4.4 | REFUTED | `git diff HEAD~1 -- internal/` returned no output for the HEAD commit. Drop 4's earlier units (4.0–4.3, 4.5) touched render/fileset/lister but those landed in prior commits; the HEAD commit is purely `cmd/rak/*` per the `git show --stat` evidence. |
| 11 | No `TestRootCmd_FlagToon` to cover the explicit `--toon` path | REFUTED on the 4.4 contract | The default-no-flag path is covered by `TestRootCmd_ReadsStdin_RendersTOONDefault`, and `resolveRenderer` provably maps both `--toon` and no-flag to the same `NewTOONRenderer()` via the `default` switch arm. A dedicated `--toon` test would be defensive (would catch a future refactor that wrongly conditioned on `flags.toon`), but its absence does not break any acceptance criterion. Recorded as a minor coverage observation, not a counterexample. |
| 12 | `runDirectory` signature drift: builder retained `w io.Writer` despite the spawn appendix omitting it | REFUTED | The spawn appendix listed `runDirectory(ctx, source, rootLabel, binary, renderer)` without `w io.Writer`. That signature is non-compilable — `renderer.RenderTree(w, ...)` requires a writer. The builder's resolution (retain `w` as the second positional parameter, matching the prior shape) is the only sensible reading; the spawn appendix was over-simplified. Worklog § "Design decisions" calls this out explicitly. All callers (production `runRoot` at root.go:163; test `runTreeFS` at root_test.go:206) compile and pass. |

### Findings (non-blocking, low severity)

- **F1 (LOW — coverage gap) — No end-to-end integration test for the default-TOON path-arg case.** The four `cmd/rak/integration_test.go` tests cover `--human` stdin, `--json` stdin, `--human <path>`, and `--json <path>`. Conspicuously absent: a `TestRootCmd_Integration_PathArg_TOONDefault` covering the most common user invocation `rak <dir>` (no flag, path arg). All three components individually pass:
  - `lister.Detect` GitLister branch — tested in `internal/lister/lister_test.go`
  - `NewTOONRenderer().RenderTree` — tested in `internal/render/render_test.go` via Unit 4.5
  - `runDirectory` + `walkAndCount` integration — tested via `runTreeFS` (which uses `NewWalkLister` + JSON renderer)
  
  But the **specific composition GitLister + TOONRenderer + path-arg** — the actual default user-facing v0.1.0 behavior — is not exercised end-to-end. No concrete failure repro; each isolated component works. Recommended (not 4.4-blocking) follow-up: add `TestRootCmd_Integration_PathArg_TOONDefault` against `cmd/rak/testdata/tree` asserting `directories`, `total`, and the four `bytes/lines/words/chars` field labels appear in the output. Sits naturally in either Unit 4.4 Round 2 polish or a follow-up drop close-out task.

- **F2 (LOW — doc drift, outside 4.4 diff) — `internal/render/render.go` package doc lists only `NewHumanRenderer` + `NewJSONRenderer`, missing `NewTOONRenderer`.** Lines 1–7 say "It exposes a single Renderer interface with constructors for each supported output format — NewHumanRenderer for laslig-backed TTY-aware human output, NewJSONRenderer for stdlib encoding/json machine output — and no enum-keyed factory." The TOON constructor is not listed. The drift originates in Unit 4.5 (TOON renderer addition), not Unit 4.4. `git diff HEAD~1 -- internal/render/render.go` is empty so this is NOT a 4.4 regression. Flagged for Unit 4.5 follow-up or a drop close-out doc-sweep, not 4.4-blocking.

- **F3 (LOW — message-text drift, outside 4.4 diff) — `ErrNoGitignoreInRepo` message ends without a terminal period.** PLAN.md F19a (line 184) specifies the sentinel message text ending with "`run rak outside the repository.`" (period). The actual code in `internal/lister/lister.go:35` ends with "`run rak outside the repository`" (no period). The commit `de7dcd3 fix(lister): restore intersentence period in errnogitignoreinrepo` addressed inter-sentence punctuation but not the terminal period. Sits inside `internal/lister`, not the 4.4 diff. Strictly a Unit 4.2 spec-vs-code drift. `errors.Is`-based callers are unaffected (the inspection contract uses `errors.Is`, not string-match). Not 4.4-blocking.

### Unknowns

None routed back to orchestrator. The three findings above are explicit, low-severity, and either non-blocking coverage gaps or out-of-4.4-scope drift items.

### Summary

Unit 4.4 is **PASS for Phase 5 close**. All 12 spawn-appendix attack surfaces REFUTED on the 4.4 contract. Three low-severity findings recorded — one coverage gap (F1) on the default-TOON path-arg integration test that the dev may want to backfill before drop close, and two doc/text drifts (F2, F3) that live outside the 4.4 diff and trace back to Units 4.5 and 4.2 respectively. `mage test` green (all packages cached `ok`); `git diff HEAD~1 -- cmd/rak/` matches the worklog's claimed shape; `git diff HEAD~1 -- internal/` empty (Drop 3 + earlier-Drop-4 spine preserved); F19 sentinel propagates per F19c with the spec-endorsed `lister: detect:` wrapper; F25 interface unchanged; integration fixture counts (20/2/4/20) verified against actual file contents.

## Hylla Feedback (Unit 4.4 Round 1)

N/A — Unit 4.4 touches `cmd/rak/{root.go,root_test.go,integration_test.go}` only, all of which are post-Drop-3-ingest. Hylla's snapshot is stale for everything in this diff. Evidence gathered via `Read` of the three changed files + `internal/lister/{lister,git,walk}.go` + `internal/render/{render,toon}.go` + `cmd/rak/main.go`, `git diff HEAD~1 -- cmd/rak/`, `git log --oneline -10`, `git show HEAD --stat`, `mage test`, Context7 queries for cobra error-prefix behavior and toon-go marshal output shape. No Hylla query was attempted because the entire 4.4 surface is post-snapshot — this matches the "Hylla ingest is drop-end only" policy and is not a Hylla miss.
