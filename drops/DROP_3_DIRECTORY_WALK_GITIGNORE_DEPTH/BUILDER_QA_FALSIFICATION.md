# DROP_3 — Builder QA Falsification

## Unit 3.0 — Round 1

- **QA:** go-qa-falsification-agent
- **Reviewed:** 2026-04-21
- **HEAD under review:** `be08d20` (`feat(deps): add go-gitignore and doublestar for drop-3`)
- **Verdict:** `pass` — no unmitigated counterexamples found.

### Attacks attempted

| # | Attack vector | Outcome |
|---|---|---|
| A1 | Did `mage addDep` wrap `go get`, or was a raw `go get` slipped in, or does it call `go mod download` / something else? | **Mitigated.** `magefile.go:88–93` shows `AddDep(module string)` is literally `sh.RunV("go", "get", module)`. The builder worklog's two `mage addDep` invocations return `go: added ...` — that is `go get`'s normal stdout, not `go mod download`. No raw `go get` line appears in the commit; the two `// indirect` requires in `go.mod` are the entries that `go get` produces when the module has no importer yet, which is exactly the unit's documented Drop 2 workflow. |
| A2 | Are the pinned versions legitimate upstream releases (no forks, no typo'd module paths like `go.gitignore` vs `go-gitignore`)? | **Mitigated.** `go.mod` uses `github.com/sabhiram/go-gitignore` (hyphen, correct). GitHub API confirms commit `525f6e181f062064d83887ed2530e3b1ba0bc95a` exists on the real `sabhiram/go-gitignore` repo with author date `2021-09-23T22:41:02Z`, matching the pseudo-version timestamp byte-for-byte. `bmatcuk/doublestar/v4@v4.10.0` is a tagged release on the real upstream repo (Context7 resolves it; Go module proxy lists v4.10.0 as the highest v4 tag). |
| A3 | Is "latest stable tags" honored, or did the resolver pick a stale fork / pre-release? | **Mitigated as worded, clarified.** `go list -m -versions` via the module proxy shows `sabhiram/go-gitignore` has published **zero** tags (empty version list returned by `proxy.golang.org/github.com/sabhiram/go-gitignore/@v/list`). A pseudo-version off `master` is the only resolvable "latest stable" for a tag-less module — the builder's own worklog note calls this out and is correct. `doublestar/v4` v4.10.0 is the highest v4 tag on the proxy (list shows v4.0.x–v4.10.0, no v4.11+ or pre-releases), so "latest stable tag" is satisfied literally. The Unit 3.0 acceptance wording "latest stable tags" should read "latest stable resolver choice" for tag-less modules; this is a documentation nit at the planner level, not a build failure, and the Phase 3 plan-QA loop already converged without flagging it. |
| A4 | Do the licenses permit inclusion? (MIT / Apache-2.0 / BSD expected; no GPL / AGPL / CDDL / proprietary.) | **Mitigated.** Both modules ship MIT (`LICENSE` headers: "The MIT License (MIT)\nCopyright (c) 2015 Shaba Abhiram" and "The MIT License (MIT)\nCopyright (c) 2014 Bob Matcuk"). MIT is compatible with any outbound license rak chooses. |
| A5 | Any CVEs on the pinned versions? | **Mitigated.** The Go vulnerability DB index at `https://vuln.go.dev/index/modules.json` (365 KB, pulled 2026-04-21) has **zero entries** matching `sabhiram`, `bmatcuk`, `doublestar`, or `gitignore` (case-insensitive). No published CVEs affect either pin. |
| A6 | Hidden compiled transitive deps pulled in by either module? | **Mitigated.** `bmatcuk/doublestar/v4@v4.10.0`'s own `go.mod` is two lines (`module github.com/bmatcuk/doublestar/v4\ngo 1.16`) — zero requires. `sabhiram/go-gitignore@v0.0.0-20210923224102-525f6e181f06`'s `go.mod` requires only `github.com/stretchr/testify v1.6.1`, a **test-only** dep that does not get compiled into rak. The four `/go.mod`-only lines added to rak's `go.sum` (`davecgh/go-spew v1.1.0`, `stretchr/objx v0.1.0`, `stretchr/testify v1.6.1`, `gopkg.in/yaml.v3 v3.0.0-20200313102051`) are module-graph closure records — Go records `.mod` hashes for the full dependency graph so `go mod verify` can check the graph, but these modules are **not** downloaded as source and **not** linked. rak's `go.mod` gained exactly two new `// indirect` lines, matching the builder's claim. |
| A7 | Does `mage ci` (not just `mage build` + `mage test`) pass — including `gofumpt -l .` emptiness and `golangci-lint run`? | **Mitigated.** Ran `mage ci` from `main/` during this review: `gofumpt -l .` clean, `golangci-lint run` reported `0 issues.`, `go test -race ./...` passed all three test packages (cached: `cmd/rak`, `internal/counting`, `internal/render`). The builder's worklog only cited `mage build` + `mage test`, but `mage ci` also passes, so nothing was swept under the rug. |
| A8 | YAGNI — does the drop actually need both deps now, or could one be deferred? | **Accepted as not-a-finding.** Unit 3.1's acceptance explicitly consumes `sabhiram/go-gitignore` (gitignore matcher) and `bmatcuk/doublestar/v4` (`--include`/`--exclude` globs), landing in the same drop. Deferring either would just push the identical `mage addDep` invocation into 3.1, creating a dep-plus-code commit that's harder to review. Coupling two adds in one unit is the Drop 2 workflow's stated purpose. |
| A9 | Did the builder sneak a raw `go` invocation anywhere (script, commit, file)? | **Mitigated.** `git show HEAD --stat` shows only four files touched: `go.mod`, `go.sum`, the drop's `PLAN.md` (state flip `todo → done`), and `BUILDER_WORKLOG.md`. No scripts, no workflow files, no magefile edits. `magefile.go` is unchanged from Drop 1.4, so the only route to these dep additions was `mage addDep` → `sh.RunV("go", "get", module)`. |
| A10 | Did the unit produce any uncommitted residue (e.g. a lingering `go mod tidy` side-effect the builder hadn't noticed)? | **Mitigated.** `git status` reports `working tree clean` at HEAD. No stray edits. Branch is 9 commits ahead of `origin/main` — consistent with Drop 3 being mid-build and push deferred to drop-end verify per WORKFLOW.md Phase 6. |
| A11 | Does `go.mod` integrity match `go.sum` — no missing or extra hashes? | **Mitigated.** `mage test -race ./...` inside `mage ci` would have failed with `missing go.sum entry` or `verifying ...: checksum mismatch` if integrity were broken; it passed clean. Both `h1:` and `/go.mod` hashes for each new module are present in go.sum. |
| A12 | Is the Go toolchain version compatible? | **Mitigated.** `go.mod` declares `go 1.26.1`; both dep `go.mod`s declare `go 1.13` (sabhiram) and `go 1.16` (doublestar), well below 1.26.1 so no forward-compat issue. |

### Counterexamples

None. No attack produced a concrete failure reproducible against HEAD.

### Findings surface

- **Documentation nit (non-blocking)** — Unit 3.0's acceptance wording "latest stable tags" reads literally-incorrectly for a tag-less module. The builder worklog calls this out and offers the correct interpretation ("latest stable resolver choice"). If the orchestrator wants tighter prose, a one-line planner tweak at drop-close would be ergonomic; it is **not** grounds for failing this QA round because the resolver-selected pseudo-version is the only legitimate choice upstream.

### Summary

Unit 3.0 is clean. Two production deps added by the mandated `mage addDep` path, both MIT-licensed, no CVEs, no hidden transitive compile surface, `mage ci` fully green. No counterexample found.

## Unit 3.1 — Round 1

- **QA:** go-qa-falsification-agent
- **Reviewed:** 2026-04-21
- **HEAD under review:** `1ef8e68` (`feat(ignore): add matcher interface with gitignore and glob`)
- **Verdict:** `pass` — no unmitigated counterexample found.

### Attacks attempted

| # | Attack vector | Outcome |
|---|---|---|
| A1 | **F1 polarity inversion** — does any code path return `true` when the path should be kept? | **REFUTED.** `compositeMatcher.Match` (ignore.go:102–113) returns true only on exclude hit, gitignore hit, or (`hasIncludes && !include.match`). Each sub-matcher honors the convention: `gitignoreMatcher.match` (gitignore.go:60–78) returns true iff `rule.compiled.MatchesPath(probe)` — `go-gitignore.MatchesPath` returns true when the path is ignored (source at `/Users/evanschultz/go/pkg/mod/github.com/sabhiram/go-gitignore@.../ignore.go:209–212`). `globMatcher.match` (glob.go:48–56) returns true on any pattern hit, used as "drop this" for exclude and "survived filter" for include via the `!` at ignore.go:109. No inverted branch. Tests `TestMatcher_Empty`, `TestMatcher_ExcludeOnly`, `TestMatcher_IncludeOnly`, `TestMatcher_GitignoreOnly` all assert `true == ignore`. |
| A2 | **F2 exclude-wins composition counterexample** — construct a case where `--include` would admit AND gitignore would `!`-re-include AND `--exclude` would drop. Does exclude still win? | **REFUTED.** `TestMatcher_Precedence_ExcludeBeatsGitignoreNegate` (ignore_test.go:218–242) sets `roots = "*.go" + "!important.go"`, `excludes = ["important.go"]`. The gitignore-negate would re-include, but `compositeMatcher.Match` short-circuits on `c.exclude.match` first — `important.go` drops. Assertion: `m.Match("important.go", false) == true`. Inspecting ignore.go:103: the exclude check runs before any gitignore call, so no sequencing bug possible. I tried to extend the attack — what about `!important.go` with `--include="important.go"` — `TestMatcher_Precedence_IncludeDoesNotOverrideExclude` (ignore_test.go:247–261) covers this too. Attack exhausted. |
| A3 | **F3 precedence ordering drift** — does the code actually run exclude → gitignore → include, or is it any other order? | **REFUTED.** ignore.go:102–113 is literally three `if` blocks in order: `c.exclude.match` → `c.gitignore.match` → `c.hasIncludes && !c.include.match`. No table-driven "rule list", no sorting, no map iteration. The order is static and source-diffable. `TestMatcher_Precedence_IncludeAfterGitignore` (ignore_test.go:267–285) pins `gitignore` before `include`. Attack cannot produce a reordering. |
| A4 | **C6 forward-slash violation** — does any code path accept, emit, or compare on `filepath.Separator` / OS-native separators? | **REFUTED.** `Grep 'filepath\.' main/internal/ignore/` → no matches. `Grep 'PathMatch' main/internal/ignore/` → one hit on line glob.go:15 in a comment explaining why `PathMatch` is rejected. Actual call at glob.go:50 is `doublestar.Match(p, name)` which Context7 + `go doc` confirm splits on `/` on all platforms ("Match returns true if name matches the file name pattern. name and pattern are split on forward slash (/) characters"). `scopePath` (gitignore.go:84–96) uses `strings.HasPrefix` and `strings.TrimPrefix` against literal `"/"` separators, not `filepath.Separator`. Forward-slash invariant holds. |
| A5 | **doublestar.Match vs PathMatch regression** — was the Round-3 correction actually committed? | **REFUTED.** Direct read of glob.go:50: `ok, _ := doublestar.Match(p, name)`. The only mention of `PathMatch` in the package is the docstring rejection note at glob.go:15. The Round-3 planner correction survived into code. |
| A6 | **Hierarchical gitignore scope leak** — can a pattern in `nested/.gitignore` match a sibling path `other/secret.conf`? | **REFUTED.** `scopePath` (gitignore.go:84–96) returns `("", false)` for out-of-scope paths (neither `relPath == dir` nor has `dir + "/"` prefix). `gitignoreMatcher.match` (gitignore.go:66–68) skips rules whose scope doesn't apply. `TestMatcher_GitignoreOnly/scoped_rule_misses_in_sibling` (ignore_test.go:91) asserts `Match("other/secret.conf", false) == false` when the rule `"secret.conf"` is scoped to `Dir: "nested"`. F8 holds. |
| A7 | **Dir-only trailing-slash probe** — does the probe accidentally match a regular file named `node_modules` when the pattern is `node_modules/`? | **REFUTED.** gitignore.go:69–72: `probe` gets the `/` suffix only when `isDir == true`. `TestMatcher_GitignoreOnly/dir_only_pattern_skips_file_of_same_name` (ignore_test.go:74) asserts `Match("node_modules", false) == false` when rule is `"node_modules/"`. Dir-only semantics correct. |
| A8 | **Negation re-include ordering** — does `*.log` + `!keep.log` actually keep `keep.log`? | **REFUTED.** `TestMatcher_GitignoreOnly/negation_reincludes` (ignore_test.go:72) pins `keep.log` as NOT ignored. The go-gitignore implementation at `/Users/evanschultz/go/pkg/mod/...ignore.go:217–237` implements the "last-matching-rule wins" semantics git uses: for each pattern, if match and non-negate → mark ignored; if match and negate and already ignored → unmark. Rak's wrapper (gitignore.go:73) just calls `rule.compiled.MatchesPath` — it inherits this behavior unchanged. |
| A9 | **Malformed glob error wrapping** — does an invalid include/exclude produce a wrapped error that unwraps to `doublestar.ErrBadPattern` via `errors.Is`? | **REFUTED.** glob.go:32–34 wraps with `fmt.Errorf("ignore: invalid glob pattern %q: %w", p, doublestar.ErrBadPattern)` — the `%w` verb preserves unwrap-ability. `TestMatcher_InvalidGlob_Include` (ignore_test.go:320–331) and `TestMatcher_InvalidGlob_Exclude` (ignore_test.go:335–345) both assert `errors.Is(err, doublestar.ErrBadPattern) == true`. Correct error wrapping per CLAUDE.md § "Errors". |
| A10 | **Empty matcher never drops** — `New(nil, nil, nil)` + any path + any isDir must return false. | **REFUTED.** `TestMatcher_Empty` (ignore_test.go:16–36) exercises four representative paths with `New(nil, nil, nil)`; all four return false. Tracing: excludeM.patterns == []; gitignoreM.rules == []; includeM.patterns == []; hasIncludes == false. All three `if` checks in `compositeMatcher.Match` short-circuit to no-op. |
| A11 | **Edge path inputs** — `""` root path, `.` root path, trailing slashes, double slashes. | **REFUTED for tested shapes; UNKNOWN for the `""` + isDir=true case.** ignore_test.go test surface does not exercise `relPath == ""`. Tracing the code manually: if `relPath = ""` and `isDir = true`, `scopePath("", "")` returns `("", true)` for a walk-root rule. Then `probe = "/"` (gitignore.go:70–72). `go-gitignore.MatchesPath("/")` is evaluated — the library normalizes separators and runs regex against `/`. For pattern `*.log`, the compiled regex (per library source line 136–138) is `^(|.*/)([^/]*)\.log(|/.*)$`. `/` does not match this expression. Benign behavior, but this code path is architecturally unreachable: the walker cannot emit an entry at `""` (the walk root itself is not yielded as a `*File` per 3.3's spec; emissions begin at depth ≥ 1). Listed under Unknowns below. |
| A12 | **Concurrency / shared mutable state** — does Drop 3.3's parallel walker expose any race? | **REFUTED.** After `New` returns, no field on `compositeMatcher`, `gitignoreMatcher`, or `globMatcher` is mutated — all reads. `go-gitignore`'s `MatchesPath` (library source line 217–237) reads `gi.patterns` and calls `regexp.Regexp.MatchString` on each — `regexp.Regexp` is documented safe for concurrent use. `doublestar.Match` is a pure function. No shared writable state. A follow-up race test in Drop 3.3's walker will be the actual gate, but the `internal/ignore` package as-shipped is race-safe. |
| A13 | **YAGNI pressure** — is any abstraction added "for later"? | **REFUTED.** `Matcher` interface has one implementation (`compositeMatcher`), but it's justified: (a) F12 requires `internal/fileset` to be CLI-free, so the interface is the fileset → ignore seam; (b) tests swap in the real `compositeMatcher`, no mock variant pre-imagined; (c) the `New` return type is `Matcher` (abstract), not `*compositeMatcher`, so callers never depend on the concrete shape. `GitignoreRoot` is a 2-field exported struct with the minimum walker needs (dir + raw lines). No `Options` builder, no `Config`, no stubbed constructors. Clean. |
| A14 | **Hidden dependency / init() side effects** — any package-level state, implicit ordering, test-order coupling? | **REFUTED.** `Grep 'func init' main/internal/ignore/` → no matches. No package-level mutable state (`var` declarations are zero — confirmed by reading all three `.go` files end-to-end). Test file uses `t.Parallel()` on every case and the suite stays green, which would catch package-global coupling via the race detector. |
| A15 | **Raw `go` command leakage** — did the unit sneak a raw `go build`/`test`/`vet` anywhere? | **REFUTED.** `git diff be08d20..HEAD --stat` shows only `internal/ignore/*.go`, `internal/ignore/ignore_test.go`, and drop mds touched. No scripts, no Makefile, no CI edits, no magefile edits. `Grep 'go (build\|test\|vet\|run\|install\|get) ' main/internal/ignore/` → no matches. |
| A16 | **`mage install` invocation** — any trace? | **REFUTED.** `Grep 'mage install'` returns only historical Drop 1/2 references (CLAUDE.md guard text, ledger notes). No new invocation in Unit 3.1's diff. |
| A17 | **File/package gating bypass** — edits outside declared `paths`? | **REFUTED.** Unit 3.1 declared `paths = main/internal/ignore/ignore.go | gitignore.go | glob.go | ignore_test.go`. Diff against `be08d20` shows exactly those four new files plus the two drop mds (PLAN.md state flip + BUILDER_WORKLOG.md append) — allowed. No out-of-scope edits. |
| A18 | **Hylla Feedback section present in worklog** — closing comment contract requires it. | **REFUTED.** BUILDER_WORKLOG.md:54–56 carries `## Hylla Feedback` with `N/A — task touched non-Go files only for reads ...`. Contract honored. |
| A19 | **`mage ci` independently green** — did the builder claim truly hold? | **REFUTED.** Ran `mage ci` from `main/` during this review: `gofumpt -l .` emitted nothing, `golangci-lint run` reported `0 issues.`, `go test -race ./...` green on all four packages (`cmd/rak`, `internal/counting`, `internal/ignore`, `internal/render`). Race detector on. |
| A20 | **Stale Round-3 planner wording survived into code** — any leftover `PathMatch`, `filepath.Match`, or OS-separator comment that contradicts the committed impl? | **REFUTED.** `PathMatch` appears only in the docstring rejection note at glob.go:15. `filepath.Match` appears nowhere in the package. Docs and code agree. |

### Counterexamples

None. No attack produced a concrete failure reproducible against HEAD.

### Findings surface

- **Unknown (non-blocking, architectural)** — The `relPath == ""` + `isDir == true` code path in `gitignoreMatcher.match` is untested. Manual trace shows benign behavior (`probe == "/"`, no realistic gitignore pattern matches), and the walker (Drop 3.3) does not emit an entry for the walk root itself, so the code path is architecturally unreachable. Listed as an Unknown, not a finding — the planner or builder may want to add an explicit test case in Drop 3.3 when the walker integration exists, or leave it as-is.

### Summary

Unit 3.1 is clean. `internal/ignore` implements F1/F2/F3/C6/F8 correctly, uses the Round-3-corrected `doublestar.Match` (not `PathMatch`), wraps malformed-glob errors via `%w` with `errors.Is` unwrap, refuses mutation after construction (concurrent-read-safe), and its test suite covers every acceptance clause. `mage ci` independently green (gofumpt clean, lint 0 issues, tests green with `-race`). No counterexample found.

## Unit 3.2 — Round 1

- **QA:** go-qa-falsification-agent
- **Reviewed:** 2026-04-21
- **HEAD under review:** `a794aee` (`feat(fileset): add file type with open, peek, and hidden helper`)
- **Verdict:** `pass` — no unmitigated counterexample found.

### Attacks attempted

| # | Attack vector | Outcome |
|---|---|---|
| A1 | **F4 stateless Peek — two calls must return identical bytes.** Any hidden state on `*File` that could drift between calls? | **REFUTED.** `Peek` (file.go:87–106) opens a fresh `rc` via `f.Open()` each call, allocates a fresh `buf := make([]byte, n)`, closes the handle on defer. `*File` carries no cursor, counter, or read-state — only `Path`, `RelPath`, `fs` (all set at construction, never mutated). `TestFile_Peek_MultipleCalls` (file_test.go:123–147) asserts two back-to-back `Peek(10)` calls return identical bytes. No drift possible. |
| A2 | **Concurrent Peek race.** Two goroutines calling `f.Peek(n)` on the same `*File` — any shared writable state that would trip `-race`? | **REFUTED.** Each call opens an independent `fs.File` handle via `f.fs.Open(f.Path)` (MapFS and `os.DirFS` both return fresh handles per call). `buf` is stack-local per call. The only shared field `f.fs` is a read-only interface value after construction. `mage test` runs with `-race` always on (per `magefile.go` `go test -race`), passed clean on the fileset package. |
| A3 | **`Peek(0)` and negative `n`.** Does `make([]byte, n)` panic on negative? | **REFUTED as correctness bug; FLAGGED as test-coverage gap.** file.go:88–90 short-circuits `n <= 0` to `(nil, nil)` before the `make([]byte, n)` call, so neither `Peek(0)` nor `Peek(-1)` panics. Behavior is correct. However, the `TestFile_Peek` table (file_test.go:65–101) does not include a `n=0` or `n<0` row — if someone removed the guard in a future refactor, no test would catch the regression. This is a coverage gap, not a shipped bug. Listed in Findings below. |
| A4 | **Short file (content shorter than n).** Does it return bytes without error, or leak `io.ErrUnexpectedEOF`? | **REFUTED.** `io.ReadFull` returns `io.ErrUnexpectedEOF` on short read (per Context7 `/websites/pkg_go_dev_go1_25_3` — "If an EOF happens after reading some but not all the bytes, ReadFull returns ErrUnexpectedEOF"). file.go:102 tolerates both `io.ErrUnexpectedEOF` and `io.EOF` via `errors.Is`, returning `(buf[:k], nil)`. `TestFile_Peek/short_file_returns_all_bytes` (content `"hi"`, n=512) asserts `got == "hi"`, `err == nil`. |
| A5 | **Exactly n bytes.** Does it return `(bytes, nil)` or `(bytes, io.EOF)`? | **REFUTED.** `io.ReadFull` returns `err == nil` when exactly `n` bytes are read (per stdlib doc — "On return, n == len(buf) if and only if err == nil"). `TestFile_Peek/exact_match_returns_all_bytes` (content `"exactly8"`, n=8) asserts `err == nil` and bytes equal. |
| A6 | **Empty file, n>0.** Does it return `(nil, nil)`, `([]byte{}, nil)`, or leak `io.EOF`? | **REFUTED.** `io.ReadFull` on an empty reader reads 0 bytes with `err == io.EOF` (explicit stdlib contract — "The error is EOF only if no bytes were read"). file.go:102 tolerates `io.EOF` via `errors.Is`, returns `buf[:0]` (empty slice over 512-byte backing). `TestFile_Peek/empty_file_returns_empty` (content `[]byte{}`, n=512) asserts `err == nil` and `bytes.Equal(got, []byte{})`. `bytes.Equal(nil, []byte{}) == true` so the test is not fragile to nil-vs-empty distinction. Minor inefficiency: `buf[:0]` retains a 512-byte backing array for empty files; this is an allocation nit, not a correctness counterexample. |
| A7 | **Non-existent file — does the error unwrap to `fs.ErrNotExist` via `errors.Is`?** | **REFUTED.** `Open` (file.go:67–73) wraps with `fmt.Errorf("open %q: %w", f.Path, err)`. The `%w` verb preserves the full unwrap chain; MapFS (and `os.DirFS`) return `*fs.PathError` carrying `fs.ErrNotExist`. `TestFile_Open_NotFound` (file_test.go:40–60) asserts both `errors.Is(err, fs.ErrNotExist) == true` and the text contains `open "missing.txt":`. Verified by running `mage test` cache-cold, green. |
| A8 | **Error wrapping format — `%q` correct for paths with spaces / special chars?** | **REFUTED.** `%q` quotes with Go syntax, producing e.g. `open "foo bar.txt":` (double-quoted). This is strictly better than `%s` for paths with embedded whitespace or control characters — unambiguous to the reader. `TestFile_Open_NotFound` asserts the literal prefix `open "missing.txt":`. No escape or quoting bug. |
| A9 | **`defer Close` error swallowing vs CLAUDE.md "never swallow" rule.** | **REFUTED.** file.go:96–98 reads `// Discard Close error: Peek has already consumed the bytes it needed, and / fs.File Close failures on a read-only handle do not invalidate the bytes / already returned. ...` followed by `defer func() { _ = rc.Close() }()`. CLAUDE.md § "Errors" permits `_ = err` **with a one-line comment explaining why**, and this has three lines of justification. Policy honored. Initial `golangci-lint` run (per worklog) flagged the bare `defer rc.Close()` via `errcheck`; the fix matches the CLAUDE.md pattern. |
| A10 | **`IsHidden` edge cases: `.`, `..`, empty string, unicode combining characters, `foo/.bar`, single `/`.** | **REFUTED.** `.` and `..` → false (line 118 special case). Empty → false (line 118 special case). `.git`, `.hidden.txt` → true (HasPrefix `.`). `foo/.bar` → false because `HasPrefix` on full string fails (first byte is `f`); caller contract (docstring at file.go:108–116) explicitly says "a single path element (a basename, not a full path)" and the Walker feeds `DirEntry.Name()`, which is basename-only. Unicode combining characters prepended (e.g. `\u0301.x`) → false because `HasPrefix` is byte-level and `\u0301`'s first byte is `0xCC`, not `0x2E`. This matches POSIX shell hidden-file semantics (byte-compare with `.`), not rune-level Unicode hidden semantics (which do not exist as a standard). Single `/` → false (neither matches special cases nor starts with `.`); out-of-contract input, but behavior is benign. All six tested rows (`TestIsHidden`) assert consistently. |
| A11 | **Zero-value `File` — does calling `Open()` on `File{}` panic or return a reasonable error?** | **REFUTED as counterexample; DOCUMENTED non-contract.** A bare `fileset.File{}` has `fs == nil`; calling `.Open()` invokes `f.fs.Open(...)` on a nil interface, which panics with "invalid memory address or nil pointer dereference". The docstring at file.go:32–35 explicitly says "Zero-value File is not useful; construct via newFile". `newFile` is unexported. Since `internal/fileset` is a Go-internal package, no caller outside the `github.com/evanmschultz/rak` module tree can construct a `File{}` with an unset `fs` through a legal path — the Walker (Unit 3.3) is the sole non-test constructor caller. Inside the rak module, any caller bypassing `newFile` to fabricate a zero `fs` has broken the documented non-contract. Not a shipped bug; documented behavior. |
| A12 | **Leaked file descriptor on panic.** If `io.ReadFull` panics mid-read, does `defer rc.Close()` still run? | **REFUTED.** Go runtime runs deferred functions during panic unwinding (language spec, `defer` statement). `rc` is acquired on file.go:91 and the `defer func() { _ = rc.Close() }()` is registered on file.go:98 — any panic between those lines would skip the defer, but there is no allocation / call between line 91 and line 98 that could panic (only the `if err != nil { return nil, err }` guard). From line 98 onward, the defer is armed and the Close will run even on panic. Handle-leak path verified clean. |
| A13 | **YAGNI — unused fields or methods.** | **REFUTED.** Fields: `Path` (used by `Open` at line 68 and in the wrapping format at line 70), `RelPath` (consumed by downstream matcher/renderer per acceptance; not used in file.go itself — intentional per plan, a contract for 3.3/3.5), `fs` (used by `Open` at line 68). Methods: `Open` (in acceptance), `Peek` (in acceptance), `IsHidden` (in acceptance). `newFile` is used by both test functions and will be used by Walker in 3.3. No speculative generalization, no interface-with-one-impl added, no `Config`/`Options` builder. |
| A14 | **Hidden dependencies / init() side effects / package-level state.** | **REFUTED.** `Grep 'func init' main/internal/fileset/` → no matches. `Grep '^var ' main/internal/fileset/file.go` → no matches. The package declares zero mutable package-level state. `t.Parallel()` is used on every test function and every table subcase (file_test.go:14, 42, 63, 105, 124, 150, 167), which would surface coupling via `-race`. Clean. |
| A15 | **File/package gating bypass — edits outside declared `paths`?** | **REFUTED.** Unit 3.2 declared `paths = main/internal/fileset/file.go, main/internal/fileset/file_test.go`. `git show a794aee --stat` lists exactly `internal/fileset/file.go`, `internal/fileset/file_test.go`, `drops/DROP_3_.../PLAN.md` (state flip), `drops/DROP_3_.../BUILDER_WORKLOG.md` (append) — no out-of-scope edits. |
| A16 | **Raw `go` command leakage anywhere in diff or worklog.** | **REFUTED.** `Grep 'go (build\|test\|vet\|run\|get\|install) ' main/internal/fileset/` → no matches. Worklog lists only `mage build`, `mage test`, `mage lint`, `mage format`, `mage ci`. No raw go invocations. |
| A17 | **`mage install` invocation anywhere in this unit.** | **REFUTED.** No trace in diff or worklog. |
| A18 | **Symbol drift — does the file actually export what Unit 3.2 acceptance names?** | **REFUTED.** Acceptance names `type File struct {…}`, `func (f *File) Open()`, `func (f *File) Peek(n int)`, `func IsHidden(name string) bool`. Code at file.go:36, 67, 87, 117 declares exactly these. Field list matches (`Path`, `RelPath`, `fs`). No silent rename. |
| A19 | **`mage ci` independently green.** | **REFUTED.** Ran `mage ci` from `main/` during this review: `gofumpt -l .` clean, `golangci-lint run` reported `0 issues.`, `go test -race ./...` green on all five packages (`cmd/rak`, `internal/counting`, `internal/fileset`, `internal/ignore`, `internal/render`). Follow-up `go clean -testcache && mage test` confirmed tests pass fresh (not just cached). |
| A20 | **Hylla Feedback section present in worklog** — closing comment contract requires it. | **REFUTED.** BUILDER_WORKLOG.md:85–87 carries `## Hylla Feedback` with `N/A — Unit 3.2 introduced a brand-new file ... in a package that did not exist in the last Hylla ingest ...`. Contract honored; justification plausible (the package was new, no Hylla data could have answered a question the builder actually asked). |
| A21 | **Interface satisfaction drift — does `Open` return type really satisfy `io.ReadCloser`?** | **REFUTED.** `f.fs.Open(f.Path)` returns `fs.File`, defined as `interface { Stat() (FileInfo, error); Read([]byte) (int, error); Close() error }` (per `go doc io/fs.File`). That is a superset of `io.ReadCloser { Read + Close }`, so the implicit interface conversion at file.go:72 (`return rc, nil`) is valid. `rc` is typed `io.ReadCloser` in the signature. Verified by `mage build` clean. |

### Counterexamples

None. No attack produced a concrete failure reproducible against HEAD `a794aee`.

### Findings surface

- **Non-blocking test-coverage gap (A3)** — `TestFile_Peek` does not include a `n=0` or negative-`n` row. The implementation's short-circuit at file.go:88–90 is correct, but no regression guard exists. If Unit 3.3 or a later drop refactors `Peek`, removing the guard would silently break; `make([]byte, -1)` would panic without a failing test. Recommended follow-up: add two table rows (`peek_zero_returns_nil`, `peek_negative_returns_nil`) to `TestFile_Peek` either here or in a Drop 3.3 sweep. Not grounds for failing this QA round — the shipped behavior is correct and the contract is documented in the `Peek` doc comment at file.go:83–85. Flagged for the orchestrator to route (dev decides: amend this unit now, roll into 3.3, or defer).

### Summary

Unit 3.2 is clean. `internal/fileset/file.go` implements the `File` type with a stateless `Peek` honoring F4, wraps `Open` errors with `%q: %w` so `errors.Is(err, fs.ErrNotExist)` holds through the chain, and exposes `IsHidden` with documented basename-only semantics matching C3. All six planned test functions are present and exercise the acceptance criteria directly; tests run with `t.Parallel()` and pass under `-race`. `mage ci` independently green (gofumpt clean, lint 0 issues, tests green cache-cold). Diff stays inside the declared `paths`. One minor non-blocking test-coverage gap on `n <= 0` (the guard exists but has no regression test). No counterexample found.

## Unit 3.3 — Round 1

- **QA:** go-qa-falsification-agent
- **Reviewed:** 2026-04-21
- **HEAD under review:** `internal/fileset/walker.go` + `walker_test.go` as appended to `BUILDER_WORKLOG.md` Unit 3.3 Round 1 (files exist on disk in the working tree; `mage ci` green end-to-end).
- **Verdict:** `pass` — no unmitigated counterexample found.

### Attacks attempted

| # | Attack vector | Outcome |
|---|---|---|
| A1 | **F14 panic guard — can yield be re-invoked after returning false?** Iter package contract (`go doc iter`) says *"Yield panics if called after it returns false."* | **REFUTED.** `yieldOK` captured bool at walker.go:122 is checked at walker.go:150 before any branch that could yield. Every yield-returning-false path flips `yieldOK = false` + returns `fs.SkipAll` (walker.go:174–175, 245–246, 269–271). `fs.WalkDir` is synchronous (stdlib doc), so the next `WalkDirFunc` invocation happens strictly after the current one returns — the guard flip is visible. `TestWalker_RangeBreak` (walker_test.go:422–457) uses a 4-file fixture, breaks after first emit, wraps a `defer recover()` that fails the test on panic, and asserts `count == 1`. `mage test -race` passes. No path invokes yield after it returned false. |
| A2 | **F14 secondary path — context-cancel yield.** `if err := ctx.Err(); err != nil { yield(nil, err); yieldOK = false; return fs.SkipAll }` at walker.go:159–163 discards the yield's return. Could that yield itself be invoked after a prior false-return? | **REFUTED.** The `if !yieldOK` guard at walker.go:150 runs **before** the ctx check at walker.go:159, so a prior false-yield forces `fs.SkipAll` without reaching the ctx-yield site. Ordering pinned by code-path inspection. |
| A3 | **F14 tertiary path — matcherErr yield.** `yield(nil, fmt.Errorf("walker: %w", matcherErr))` at walker.go:192 also discards the yield return. Re-invocation risk? | **REFUTED.** Same guard ordering: the `yieldOK` check at line 150 dominates all yield sites. `yieldOK = false` is set on line 193 immediately after that discarded yield, so any hypothetical re-entry would be caught by the guard on the next invocation. |
| A4 | **F14 quaternary path — defensive outer yield** at walker.go:281 (`if err != nil && yieldOK { _ = yield(nil, fmt.Errorf("walker: %w", err)) }`). This is outside `fs.WalkDir`'s callback loop. Could yield re-invoke? | **REFUTED.** The outer closure is itself a single invocation of the iterator function passed to `range`. After this final yield, the closure returns and the iterator sequence ends. Go's `range` protocol over `iter.Seq2` does not re-invoke after the function returns. The `&& yieldOK` conjunct additionally gates on the same guard. Safe. |
| A5 | **F5 iter.Seq2 signature** — does `Walk` actually return `iter.Seq2[*File, error]`? | **REFUTED.** walker.go:116 signature: `func (w *Walker) Walk(ctx context.Context) iter.Seq2[*File, error]`. Body at line 117 returns `func(yield func(*File, error) bool)` — exactly the shape `iter.Seq2[*File, error] = func(yield func(*File, error) bool)` per `go doc iter.Seq2`. Range-over-func in tests (`for f, err := range w.Walk(ctx)`) compiles and runs, confirming runtime conformance. |
| A6 | **F6 error resilience — unreadable directory continues walk.** Can multiple sequential broken dirs all propagate? | **REFUTED.** walker.go:171–184 yields the wrapped error and returns `fs.SkipDir` (when `d != nil && d.IsDir()`) or `nil` (else), letting `fs.WalkDir` continue past the failed subtree. `TestWalker_UnreadableEntry` (walker_test.go:383–420) uses a custom `errFS` stub whose `Open("broken")` returns an `errDir` failing every `ReadDir`. Test asserts the induced error is yielded AND `keep.txt` + `other/y.txt` still emit. Multiple consecutive broken dirs would each trigger the same path independently — each is handled in isolation in the callback. |
| A7 | **F7 symlink policy — any `os.Stat` / `Readlink` call that would accidentally follow?** | **REFUTED.** `Grep 'Stat\|Readlink\|Lstat'` against `walker.go` returns zero matches. Only `fsys.Open` (via `readGitignore` at walker.go:322) and `fs.WalkDir` (stdlib — documented "does not follow symbolic links") cross the filesystem boundary. `TestWalker_SymlinkYielded` (walker_test.go:459–510) asserts both `link_ok` and `link_broken` are yielded as ordinary entries and `link_broken.Open()` unwraps to `fs.ErrNotExist`. No symlink-loop trap is possible because the walker never follows a link — `fsys.Open` on a MapFile with `fs.ModeSymlink` just reads its target-path `Data`, not the target. |
| A8 | **F8 hierarchical gitignore — sibling scope.** A pattern in `foo/.gitignore` matching a path like `foo/../sibling/x.txt` — rejected? | **REFUTED.** `ignore.scopePath` (gitignore.go:84–96) strips the rule's `dir + "/"` prefix; if the path does not start with that prefix, returns `(_, false)` and the rule is skipped. A path `sibling/x.txt` is not in-scope for the `foo/` rule because it does not start with `foo/`. `TestWalker_NestedGitignore` (walker_test.go:247–275) pins this: `sub/.gitignore` says `secret.txt`, but `secret.txt` at the root and `other/secret.txt` in a different subtree are both emitted. No path-traversal via `..` is reachable: `fs.WalkDir` never produces paths containing `..`; it descends literal forward-slash segments, so a "`foo/../sibling`" form never appears in practice. |
| A9 | **C7 Depth semantics — exact boundary.** `Depth == 1` walks root only; `Depth == 2` root + one level. Verify against planner's pin. | **REFUTED.** `TestWalker_DepthLimit` has three rows: `depth_zero_unlimited` → all three files; `depth_one_root_only` → `[root.txt]`; `depth_two_root_plus_one_level` → `[root.txt, sub/mid.txt]`. Walker code trace: `Depth == 1`, root `"."` exempt (line 223 `p != w.root`), `"sub"` dir has `depth = 0 - 0 = 0`, `0 >= 1` false → descends; `"sub/mid.txt"` file has `depth = 1 - 0 = 1`, `1 >= 1` true → file-branch returns `nil` (not yielded). Matches the planner's `"root/file.txt is depth 0; root/sub/file.txt is depth 1"` convention exactly. Nit: the walker does descend into `sub/` even with `Depth == 1`, then suppresses emission — this is semantically equivalent to "walk only root" from the caller's perspective (no emissions from subdirs) but technically reads every subdir's gitignore. That is observable only on a test that inspects side-effects; acceptance tests only check emissions, and the planner's contract in PLAN.md line 78 is "walk only the root directory (no subdirs)" in terms of emitted output, which holds. Documenting as nit only. |
| A10 | **Context cancel mid-walk — does the walker terminate cleanly?** | **REFUTED.** `ctx.Err()` check at walker.go:159 fires on every `WalkDirFunc` invocation before any emission path. On cancel: yield `(nil, ctx.Err())`, flip `yieldOK = false`, return `fs.SkipAll`. `TestWalker_ContextCancelled` (walker_test.go:322–347) pre-cancels the ctx and asserts zero paths + at least one yielded error unwrapping to `context.Canceled`. Mid-walk cancel (ctx canceled after N emits) is structurally identical: the next `WalkDirFunc` invocation sees `ctx.Err() != nil` and stops. No test exercises the mid-walk timing explicitly, but the code path is the same. **Accepted as structurally sound.** |
| A11 | **Hidden dir with gitignore inside (IncludeHidden=true + `.git/.gitignore`)** — do patterns activate? | **REFUTED (by code trace).** With `IncludeHidden=true`, the hidden-check at walker.go:203 is bypassed. Walker descends into `.git/`, hits line 237 (`isDir && !DisableGitignore`), calls `readGitignore(fsys, ".git", ".git")` which opens `.git/.gitignore`, appends a `GitignoreRoot{Dir: ".git", Patterns: ...}`, rebuilds matcher. Subsequent entries under `.git/` are scoped against that rule. No test covers this exact combination, but the code path is structurally identical to `TestWalker_NestedGitignore`'s `sub/.gitignore` case with the directory name changing. **Not a counterexample.** |
| A12 | **Empty root `""`** — does walk handle `NewWalker(fsys, "", opts).Walk()`? | **REFUTED as counterexample; contract note.** `fs.ValidPath("")` returns false (`go doc io/fs.ValidPath`), so `fs.WalkDir(fsys, "", ...)` returns an error like `invalid argument`. The walker's callback is never invoked — the error comes back from the outer `fs.WalkDir` call. Code path at walker.go:280–282 catches `err != nil && yieldOK` and yields one `walker: ...: invalid argument` error. Graceful, not fatal. `NewWalker` does no root validation (documented at walker.go:82–84: "NewWalker does no validation of root beyond what fs.WalkDir will do at walk time"). No panic, no crash. Consistent with the documented contract. |
| A13 | **Missing root — `NewWalker(fsys, "nonexistent", opts).Walk()`.** | **REFUTED.** `fs.WalkDir` calls `fsys.Open("nonexistent")`; MapFS returns `fs.ErrNotExist`. `WalkDirFunc` is invoked with `entryErr != nil` + `d == nil` (per stdlib behavior: the first call on root with a Stat failure has nil DirEntry). Code path at walker.go:171–184: yields the wrapped error (`walk "nonexistent": ...`), then `d != nil && d.IsDir()` is false (d is nil), so returns `nil`. Walk terminates naturally because no entries exist. Caller sees exactly one yielded error and zero paths. |
| A14 | **Concurrency — range over Walk() in parallel goroutines on the same Walker.** | **REFUTED.** `Walker` struct (walker.go:72) holds `fsys`, `root`, `opts` — none mutated after `NewWalker`. Each `Walk()` call at walker.go:116 returns a **fresh closure** with its own `yieldOK`, `roots`, `matcher`, `matcherErr`, `rootDepth` locals (lines 118–144). No shared mutable state across Walk invocations. Thread-safety reduces to `fsys.Open`'s thread-safety, which `fstest.MapFS` and `os.DirFS` both guarantee. `mage test -race` is on by default (per `main/CLAUDE.md` § "Build Verification") and passes green. Concurrent Walk is safe. |
| A15 | **`.gitignore` itself yielded? (IncludeHidden=false default vs IncludeHidden=true).** | **REFUTED.** With default `IncludeHidden=false`: `.gitignore` is a hidden file (`IsHidden(".gitignore")` → true), filtered at walker.go:203 before any emission. Tests `TestWalker_Gitignore` and `TestWalker_NestedGitignore` both confirm — expected `paths` lists never include `.gitignore`. Gitignore reading still fires (via `readGitignore` at walker.go:238, which is called on the *directory* visit, not on the `.gitignore` entry itself), so the ordering "hidden filter skips emission, gitignore still read" works. With `IncludeHidden=true`: `.gitignore` would be yielded as a regular file. No test covers this, but the code path is straightforward (hidden check is bypassed, and the file is a regular file so it emits). **Not a counterexample.** |
| A16 | **YAGNI — unused fields on WalkOptions or Walker?** | **REFUTED.** `WalkOptions` fields: `Depth` (line 216–232), `IncludeHidden` (line 203), `DisableGitignore` (line 237), `Includes` (lines 137, 240), `Excludes` (lines 137, 240) — every field read. `Walker` fields: `fsys` (line 146, 238), `root` (lines 144, 146, 197, 203, 223), `opts` (multiple). No dead fields, no speculative generalization, no interface-with-one-impl. |
| A17 | **Doc comment rule 11 violations on any exported identifier.** | **REFUTED.** Exported identifiers in walker.go: `WalkOptions` (doc at line 16), `Depth` / `IncludeHidden` / `DisableGitignore` / `Includes` / `Excludes` (field docs at lines 40–58), `Walker` (line 61), `NewWalker` (line 78), `Walk` (line 89). Every doc comment starts with the identifier name. `golangci-lint run` (inside `mage ci`) reports `0 issues.` — the `revive` / `staticcheck` presets would catch a missing doc. |
| A18 | **Doublestar `Match` vs `PathMatch`** — confirm `Match` is used (per C6). | **REFUTED.** `Grep 'doublestar\.'` in `main/internal` returns only `doublestar.Match`, `doublestar.ValidatePattern`, `doublestar.ErrBadPattern`. **Zero** references to `doublestar.PathMatch`. The walker delegates to `ignore.Matcher.Match` which delegates to `globMatcher.match` which uses `doublestar.Match` (glob.go:50). C6 and planner's explicit direction honored. |
| A19 | **Raw `go` command leakage — any `go build` / `go test` / `go run` / `go vet` in the Unit 3.3 diff?** | **REFUTED.** `Grep 'go (build\|test\|vet\|run\|get\|install) '` against `main/internal/fileset/` returns zero matches. Worklog lists only `mage build`, `mage test`, `mage lint`, `mage format`, `mage ci`, `mage coverage`. No raw go invocations. |
| A20 | **`mage install` invocation anywhere in this unit.** | **REFUTED.** `Grep 'mage install'` against the drop dir returns only the prior Unit 3.0/3.1/3.2 guard-refuted rows in this same file. No new `mage install` call in Unit 3.3's diff or worklog. Dev-only target not invoked by any agent. |
| A21 | **Error swallowing — `_ = f.Close()` in `readGitignore` (walker.go:326).** Justified? | **REFUTED.** One-line justification pattern per CLAUDE.md § "Errors": the comment at walker.go:326 is inline (`defer func() { _ = f.Close() }()`) without a separate explanatory line — but the function-level docstring at walker.go:310–319 explicitly calls out "any read error are swallowed silently — a missing .gitignore is expected, and a permission error on one .gitignore should not abort the walk." This covers both the `ReadFrom` error swallow (line 332) and the scanner error swallow (line 342). The `f.Close()` swallow is the standard defer-close pattern that `errcheck` would flag without the `_ =`, and `golangci-lint run` in `mage ci` passes clean, meaning the linter accepts this shape. **Accepted as documented.** (Nit: the `_ = f.Close()` line itself has no inline comment, unlike file.go:96–98's treatment of the analogous Close. A `// read-only handle: bytes already consumed` comment on the line would bring parity, but the function-level docstring already carries the justification. Not grounds for fail.) |
| A22 | **Error wrapping — does walker use `%w` consistently so `errors.Is` walks the chain?** | **REFUTED.** Three wrap sites: walker.go:172 (`fmt.Errorf("walk %q: %w", p, entryErr)`), walker.go:192 (`fmt.Errorf("walker: %w", matcherErr)`), walker.go:244 (`fmt.Errorf("walker: %w", err)`), walker.go:281 (`fmt.Errorf("walker: %w", err)`). Every site uses `%w`. `errors.Is` will walk through the wrap to the underlying cause (including `context.Canceled`, `fs.ErrNotExist`, `doublestar.ErrBadPattern`). `TestWalker_ContextCancelled` uses `errors.Is(got.errs[0], context.Canceled)` and passes. |
| A23 | **Goroutine leaks — does the walker spawn any goroutines?** | **REFUTED.** `Grep 'go func\|go w\.\|go fs\.' main/internal/fileset/walker.go` returns zero matches. The iterator function runs synchronously inside the caller's goroutine (range-over-func is a stack transformation, not a goroutine spawn). `fs.WalkDir` is documented-synchronous. No leak surface. Parallel walking is explicitly deferred to Drop 8.1 (PLAN.md § "Deferred items"). |
| A24 | **Interface satisfaction drift — does `Walk` really return `iter.Seq2[*File, error]` that the Go runtime accepts for range?** | **REFUTED.** `mage build` clean — if the signature were wrong, the test code (`for f, err := range w.Walk(context.Background())` at walker_test.go:30, 443, 490) would fail type-check. Go 1.26's range-over-func iterator requires `func(yield func(K, V) bool)` shape; the returned value matches that exactly. |
| A25 | **File/package gating bypass — edits outside `main/internal/fileset/walker.go`, `main/internal/fileset/walker_test.go`?** | **REFUTED.** Unit 3.3 declared paths: `walker.go` + `walker_test.go`. `git status` shows modifications limited to these two new files plus the two coordination mds (`PLAN.md` state-flip, `BUILDER_WORKLOG.md` append) — both coordination mds are orchestrator/builder-managed per WORKFLOW.md, not gated by unit `paths`. No edits to `file.go`, `ignore/*`, `counting/*`, `render/*`, `cmd/rak/*`, or `magefile.go`. |
| A26 | **`mage ci` independently green.** | **REFUTED.** Ran `mage ci` from `main/` at review start: `0 issues.` from `mage lint`; `ok` on all five test packages (`cmd/rak`, `internal/counting`, `internal/fileset`, `internal/ignore`, `internal/render`) with race detector on; `gofumpt -l .` empty. End-to-end green. |
| A27 | **Hidden dependency / init() side effects / package-level mutable state.** | **REFUTED.** `Grep 'func init\|^var ' main/internal/fileset/walker.go` returns zero matches. No package-level mutable state. Each `Walk` call carries its own locals; the `Walker` struct's three fields are set in `NewWalker` and never mutated. Tests use `t.Parallel()` on every function, and the race detector is on by default. Clean. |
| A28 | **Planner-contract drift — does walker.go actually expose what Unit 3.3 acceptance named?** | **REFUTED.** Acceptance names `type WalkOptions struct { Depth int; IncludeHidden bool; DisableGitignore bool; Includes []string; Excludes []string }` → matches walker.go:39–59 field-for-field. Names `type Walker struct { ... }` + `func NewWalker(fsys fs.FS, root string, opts WalkOptions) *Walker` → matches walker.go:72, 85. Names `func (w *Walker) Walk(ctx context.Context) iter.Seq2[*File, error]` → matches walker.go:116. No symbol drift. |
| A29 | **Hylla Feedback section present in worklog** — closing comment contract requires it. | **REFUTED.** BUILDER_WORKLOG.md:123–125 carries `## Hylla Feedback` with `N/A — Unit 3.3 added a brand-new walker.go to a package whose last ingest held only file.go ... Zero Hylla queries issued, zero misses to report.` Contract honored; justification plausible (the package was freshly-authored and not yet reingested). |
| A30 | **Depth with non-`.` root (e.g. root = `"sub"`)** — does depth math still add up? | **REFUTED.** `rootDepth = slashCount("sub") = 0`. `"sub"` itself: `p == w.root`, exempt. `"sub/a.txt"`: `slashCount("sub/a.txt") = 1`, `1 - 0 = 1`. If `Depth == 1`, file branch: `1 >= 1` → skipped. If `Depth == 2`, file branch: `1 >= 2` false → yielded. `"sub/b/c.txt"`: `slashCount = 2`, `2 - 0 = 2`. With `Depth == 2`, file branch skips. Matches the planner's depth-from-walk-root convention. No test covers this exact shape (all tests use root `"."`), but the `slashCount` / `rootDepth` math is straightforward and matches documented behavior. **Accepted as structurally correct.** |
| A31 | **Matcher rebuild error path — can `ignore.New` fail on an already-valid matcher rebuild?** (walker.go:240) | **REFUTED.** `ignore.New` validates `includes` / `excludes` (glob.go:32 via `doublestar.ValidatePattern`) once per call — the same validation already ran at walker.go:137 during initial construction. If includes/excludes were valid on the first call, they are still valid on every rebuild (the slice is unchanged). The `err != nil` branch at walker.go:241 is therefore theoretically unreachable for a walker whose initial `ignore.New` succeeded — it is defensive. Worklog acknowledges this at line 104. Defensive branch is not a bug; it ensures future refactors don't silently drop the error. |
| A32 | **Gitignore root stack growth — could `roots` slice grow unbounded and matter for memory?** | **REFUTED.** Each directory with a `.gitignore` appends one `GitignoreRoot`. Compilation is linear per pattern. For a repo with N directories containing `.gitignore`, the slice holds N entries. Real-world upper bound: Linux kernel has ~30 `.gitignore` files. Even a pathological 10,000 `.gitignore` files would use a few MB — not a memory risk. Per-walk, not leaked across walks. |
| A33 | **Race between ctx-cancel yield and yield-false.** If the caller breaks mid-range AND the ctx is already cancelled, does the walker do the right thing? | **REFUTED.** Two consecutive `WalkDirFunc` invocations: if yield-false has already flipped `yieldOK=false`, the ctx check at line 159 is never reached (the `!yieldOK` early-return at line 150 dominates). If ctx-cancel fires first, `yieldOK=false` is set at line 161, so subsequent invocations exit at line 150. Either order terminates cleanly. |
| A34 | **`relFrom` with a non-`.` root that happens to be a substring of the path.** E.g. root `"a"`, path `"ab/c.txt"`. Does `strings.TrimPrefix("ab/c.txt", "a/")` misbehave? | **REFUTED.** `strings.TrimPrefix("ab/c.txt", "a/")` → `"ab/c.txt"` (no prefix match, unchanged). Correct: `"ab/c.txt"` is not under `"a"` in the tree sense. In practice `fs.WalkDir` descending root `"a"` would only emit paths starting with `"a/"` or equal to `"a"` — it never visits sibling `"ab/"`. The defensive unchanged-return is structurally inconsequential but semantically correct. |

### Counterexamples

None. No attack produced a concrete failure reproducible against the HEAD state of `walker.go` + `walker_test.go`.

### Findings surface

- **Non-blocking documentation nit (A9)** — The planner contract in PLAN.md line 78 reads "Depth == 1 means walk only the root directory (no subdirs)". The walker's actual behavior is to descend into subdirs at depth 0 (because `depth == 0` is not `>= 1`) and suppress file emissions when their depth crosses the limit. This is observationally equivalent to the planner's contract from the caller's perspective (no files from subdirs are emitted) but technically reads every subdir's `.gitignore` before pruning. Not a bug; semantically safe; nit flagged for the orch's doc-update pass at drop-close if they want tighter prose.
- **Non-blocking comment parity (A21)** — `walker.go:326` has a bare `defer func() { _ = f.Close() }()` without an inline comment, unlike `file.go:96–98` which carries a multi-line justification. The function-level docstring at walker.go:310–319 does cover the broader "swallow read errors" rationale. Parity nit only.
- **Non-blocking test-coverage observation (A10, A15)** — No test exercises mid-walk ctx cancel (only pre-cancel) or `.gitignore` emission with `IncludeHidden=true`. Both code paths are structurally sound by inspection; a future sweep could add targeted rows. Not grounds for fail.
- **Non-blocking defensive code note (A31)** — `walker.go:240–247` rebuilds the matcher on every `.gitignore` discovery and handles an `err != nil` return that is theoretically unreachable (inputs were already validated). Defensive-only, documented in the worklog, not a bug.

### Summary

Unit 3.3 is clean. `internal/fileset/walker.go` implements the F14 yield-false guard correctly via a captured `yieldOK` bool checked at every `WalkDirFunc` entry, emits `iter.Seq2[*File, error]` matching F5, yields per-entry errors without aborting the walk per F6, never follows symlinks per F7, scopes hierarchical `.gitignore` correctly per F8 (confirmed via `ignore.scopePath`), and honors C7 depth-from-walk-root semantics. The 12 test functions exercise all of the acceptance-required cases (EmptyRoot, SingleFile, NestedTree, DepthLimit × 3, SkipsHidden × 2, Gitignore × 2, NestedGitignore, IncludeExclude × 2, ContextCancelled, UnreadableEntry, RangeBreak, SymlinkYielded), run with `t.Parallel()` under `-race`, and pass green. No raw `go` invocations, no `mage install`, no doc-comment violations, no out-of-path edits, no hidden goroutines, no package-level mutable state. `mage ci` green independently. Four minor non-blocking nits surfaced (planner-prose, comment-parity, two test-coverage observations, one defensive-code note) — none are counterexamples. No concrete counterexample produced across 34 attacks.

### Hylla Feedback

N/A — Unit 3.3 review touched newly-authored Go files (`walker.go`, `walker_test.go`) in a package whose last Hylla ingest did not yet contain them. All in-rak symbols consulted (`File`, `newFile`, `IsHidden`, `ignore.Matcher`, `ignore.New`, `ignore.GitignoreRoot`, `ignore.scopePath`, `globMatcher.match`) were resolved via `Read` against the working-tree source. Stdlib semantics (`iter.Seq2`, `fs.WalkDir`, `fs.ValidPath`, `fs.DirEntry`, `fs.ReadDirFile`, `io.ReadFull`) were resolved via `go doc`. External library semantics (`doublestar.Match` vs `PathMatch`, `sabhiram/go-gitignore.MatchesPath`) were previously validated in Units 3.0 / 3.1 reviews; no fresh query needed here. No Hylla queries issued for this review; no misses to report.

## Unit 3.4 — Round 1

- **QA:** go-qa-falsification-agent
- **Reviewed:** 2026-04-21
- **HEAD under review:** `0645594` (`feat(fileset): add isbinary via peek(512) nul-byte test`)
- **Verdict:** `pass` — no unmitigated counterexample found.

### Attacks attempted

| # | Attack vector | Outcome |
|---|---|---|
| A1 | **F9 — is `ErrBinaryFile` declared per CLAUDE.md § "Errors" as a sentinel via `errors.New`?** | **REFUTED.** binary.go:15 `var ErrBinaryFile = errors.New("binary file")` — standard `errors.New` sentinel, no fmt string, no package-level formatting. Name matches `ErrFoo` rule. Inspection contract (`errors.Is`, never string-match) spelled out in the doc comment at binary.go:8–14. |
| A2 | **F9 — is the sentinel dead code within Unit 3.4?** `IsBinary` itself never returns `ErrBinaryFile`. Is that a YAGNI violation or planned scaffolding? | **REFUTED.** PLAN.md line 167 (F9 pin) names the call site: `cmd/rak/root.go`'s aggregation loop in Unit 3.5. Unit 3.5 is `blocked_by` Unit 3.4 — the sentinel cannot live in 3.5 because 3.5 imports `fileset` to reference it. Declaring it in 3.4 alongside `IsBinary` is the documented Drop 3 decomposition. Worklog line 142 explicitly calls out "Doc comment on `ErrBinaryFile` spells this out so future readers don't wonder why the sentinel is unused within the package." Planned scaffolding, one unit away from real use. Not YAGNI. |
| A3 | **F10 — NUL byte past byte 512 triggers binary classification (false positive)?** Does the implementation sniff beyond the peek window? | **REFUTED.** binary.go:41 calls `f.Peek(512)` which returns at most 512 bytes (file.go:100 `buf := make([]byte, 512)`, line 103 `return buf[:k], nil` with `k ≤ n`). `bytes.IndexByte(peek, 0x00)` scans only the returned slice. `TestFile_IsBinary/nul_past_peek_window_is_not_binary` (binary_test.go:66–69) uses a 521-byte fixture with NUL at index 520; test asserts `want: false`. F10 regression guard honored. |
| A4 | **Peek returning "up to n" — if a short file returns fewer than 512 bytes, does `IndexByte` behave correctly?** | **REFUTED.** `bytes.IndexByte(peek, 0x00)` on any `[]byte` slice (even length 0 or < 512) returns the first NUL index or `-1`. No bounds issue. `TestFile_IsBinary/nul_prefixed_buffer_is_binary` (content length 4) asserts `want: true`; `pure_ascii_hello_world_is_not_binary` (length 11) asserts `want: false`. Both short-file paths covered. |
| A5 | **Empty file — does `IsBinary` panic or return a spurious error?** | **REFUTED.** binary.go:45–47 explicit `if len(peek) == 0 { return false, nil }` guard. `TestFile_IsBinary/empty_file_is_not_binary` (content `[]byte{}`) asserts `got == false`, `err == nil`. Even if the guard were removed, `bytes.IndexByte([]byte{}, 0x00)` returns `-1`, so `-1 >= 0` is `false` — the fallback behavior is also correct. Defense-in-depth. |
| A6 | **F11 — any binary fixtures on disk in `testdata/`?** | **REFUTED.** `internal/fileset/testdata/` does not exist (verified via `ls`). `internal/fileset/` directory listing shows only `binary.go`, `binary_test.go`, `file.go`, `file_test.go`, `walker.go`, `walker_test.go` — zero fixture files. `cmd/rak/testdata/` also does not exist yet (slated for Unit 3.5 per PLAN.md line 145). All binary content in the test table is built inline via `[]byte` literals + the `buildASCII` / `buildASCIIThenNULAt` helpers. F11 satisfied. |
| A7 | **F12 — does `binary.go` import cobra / fang / laslig / flags? Is `internal/fileset` still CLI-free?** | **REFUTED.** binary.go imports are `bytes` + `errors` (lines 3–6), stdlib only. `Grep 'spf13\|cobra\|laslig\|flag\b\|fang'` against `main/internal/fileset/` returns zero matches in any `.go` file; the only hits are documentation references inside `walker.go` / `walker_test.go` comments ("The --no-gitignore CLI flag", "hidden_included_on_flag" test name), not imports. `mage build` clean. F12 holds. |
| A8 | **JPEG magic bytes `\xFF\xD8\xFF` (no NUL in first three bytes) — does classifier correctly return false per F10 spec?** | **REFUTED by spec trace, not test-covered directly.** F10 is an intentional git-compatibility choice: NUL-byte test only. A 3-byte JPEG prefix `[0xFF, 0xD8, 0xFF]` has no 0x00 — `bytes.IndexByte` returns `-1` → false. This matches git's behavior (git's `buf_is_binary` uses the same NUL-byte gate; a JPEG with no NUL in the first 8000 bytes would also be misdetected by git as "not binary"). The test table does not include a pure-`\xFF\xD8\xFF` case, but the structural behavior is equivalent to `pure_ascii_hello_world_is_not_binary` — both return false via the "no NUL in window" path. Realistic JPEGs have many 0x00 bytes in the JFIF header; the planner's spec explicitly accepts the JPEG-header-only edge case as part of the git-compatible heuristic. Not a counterexample. |
| A9 | **Error propagation — does `IsBinary` wrap `Peek` errors or return them raw? Does the wrapping honor `errors.Is(err, fs.ErrNotExist)` through the chain?** | **REFUTED.** binary.go:42–44 `if err != nil { return false, err }` — raw propagation (no wrapping). `Peek` already wraps with `open %q: %w` at file.go:105. Additional wrapping at the `IsBinary` boundary would add noise without information (C10 — "no other error paths"). The unwrap chain remains intact: `IsBinary` returns the same error value Peek returned, so `errors.Is(err, fs.ErrNotExist)` walks through to the underlying cause. Unit 3.2's `TestFile_Open_NotFound` proves Peek's wrap unwraps correctly; `IsBinary` is a passthrough. |
| A10 | **Doc comments per CLAUDE.md rule 11 — every exported identifier has a `// Name …` doc comment starting with the identifier?** | **REFUTED.** Exported identifiers in `binary.go`: `ErrBinaryFile` (doc at lines 8–14, starts with `// ErrBinaryFile is the sentinel…`) and `IsBinary` (doc at lines 17–39, starts with `// IsBinary reports whether…`). Both start with the identifier name. `golangci-lint` via `mage ci` reports `0 issues.` — `revive`/`staticcheck` preset would flag a missing doc. |
| A11 | **Concurrency — race on concurrent `IsBinary` calls on the same `*File`?** | **REFUTED.** `IsBinary` calls `f.Peek(512)` which opens a fresh `fs.File` handle per call (file.go:91 `rc, err := f.Open()` → `f.fs.Open(f.Path)`). `*File` carries no mutable state (`Path`, `RelPath`, `fs` are all set once in `newFile` and never mutated — verified at file.go:36–46). `fstest.MapFS.Open` and `os.DirFS.Open` both return fresh handles per call, safe for concurrent use. `bytes.IndexByte` is a pure function. `mage test` runs with `-race` default-on (per CLAUDE.md § "Build Verification") and passes clean. Two goroutines calling `f.IsBinary()` on the same `*File` would each perform an independent open-read-close cycle — no shared writable state to race on. F4 stateless-Peek contract (verified in Unit 3.2 Round 1, A1/A2) directly implies IsBinary race-safety. |
| A12 | **YAGNI — any unused helpers or speculative abstractions?** | **REFUTED.** Production file (`binary.go`): exactly one type-less variable (`ErrBinaryFile`) + one method (`IsBinary`). No interface, no builder, no `Options` struct, no stub for future features. Test helpers (`buildASCII`, `buildASCIIThenNULAt`) are both used at binary_test.go:33 + binary_test.go:62 — no dead helpers. `buildASCIIThenNULAt` internally calls `buildASCII`, so both exercise at test time. Zero YAGNI surface. |
| A13 | **F10 stated but reversed — does the code ever sniff past 512 via loop, slice-grow, or re-Peek?** | **REFUTED.** binary.go:40–49 is 10 lines: one `Peek(512)` call, one error guard, one empty-guard, one `bytes.IndexByte` call. No loop, no re-Peek, no `ReadAll`, no buffered read. The function body is linear and auditable. |
| A14 | **Edge: `Peek` succeeds but returns a nil slice (not empty — distinct from `[]byte{}`).** Does `len(nil) == 0` and `bytes.IndexByte(nil, 0x00)` both handle it? | **REFUTED.** `len(nil)` on a `[]byte` is `0` (Go spec: "For a slice, … the value of len is zero"), so the guard at binary.go:45 catches it. Even if the guard were removed, `bytes.IndexByte(nil, 0x00)` returns `-1` (pure function over a zero-length input). Double-safe. `Peek(512)` on an empty file returns a `buf[:0]` slice (not nil — file.go:100 allocates `buf` first) — which is still `len == 0`, hitting the same guard. No difference in behavior. |
| A15 | **Raw `go` command leakage in Unit 3.4 diff?** `go build` / `go test` / `go vet` / `go run` / `go install` / `go get`. | **REFUTED.** `git show 0645594 --stat` lists 4 files: `binary.go`, `binary_test.go`, `PLAN.md`, `BUILDER_WORKLOG.md`. `Grep 'go (build\|test\|vet\|run\|get\|install) '` against the two production files returns zero matches. Worklog lists only `mage test`, `mage lint`, `mage ci`, `mage coverage`. No raw go invocations. |
| A16 | **`mage install` invocation — any trace?** | **REFUTED.** `Grep 'mage install'` against the Unit 3.4 diff files returns zero new matches. Only historical guard-text references in CLAUDE.md and prior QA rounds. No dev-only target invoked by agent. |
| A17 | **File/package gating bypass — edits outside declared `paths`?** | **REFUTED.** Unit 3.4 declared paths: `main/internal/fileset/binary.go`, `main/internal/fileset/binary_test.go`. `git show 0645594 --stat` shows exactly those two new files plus the two coordination mds (`PLAN.md` state flip `todo → in_progress → done`, `BUILDER_WORKLOG.md` append) — both coordination mds are builder-managed per WORKFLOW.md Phase 4, not gated by unit paths. No edits to `file.go`, `walker.go`, `ignore/*`, `cmd/rak/*`, `magefile.go`. |
| A18 | **`mage ci` independently green.** | **REFUTED.** Ran `mage ci` from `main/` during this review: `gofumpt -l .` empty, `golangci-lint run` reported `0 issues.`, `go test -race ./...` green on all five packages (`cmd/rak`, `internal/counting`, `internal/fileset`, `internal/ignore`, `internal/render`). Also ran fresh-cache `mage test` (cache cleared first); all five packages green with race detector on. |
| A19 | **Hidden dependencies / init() / package-level mutable state.** | **REFUTED.** `Grep 'func init\|^var '` on `binary.go`: one match — `var ErrBinaryFile = errors.New("binary file")` at line 15. This is an immutable sentinel (`errors.New` returns a `*errorString` whose message field is never mutated). No `init()` function. No mutable package-level state. `t.Parallel()` on every test function and subcase; race detector passes clean. |
| A20 | **Interface satisfaction drift — does `IsBinary` shadow / rename / mismatch the planner's named symbol?** | **REFUTED.** Acceptance (PLAN.md line 114) names `func (f *File) IsBinary() (bool, error)`. Code at binary.go:40 declares exactly this signature. Return-tuple shape: `(bool, error)`. No silent rename, no receiver-type drift (method is on `*File` per acceptance). |
| A21 | **Hylla Feedback section present in worklog** — closing comment contract requires it. | **REFUTED.** BUILDER_WORKLOG.md:154–156 carries `## Hylla Feedback` with `N/A — Unit 3.4 added a new binary.go to internal/fileset, a package whose current Hylla snapshot still predates Drop 3 …`. Contract honored; justification consistent with prior 3.2/3.3 rationale (package freshly authored, not yet reingested). |
| A22 | **Doc comment on the sentinel's semantic drift** — does the ErrBinaryFile doc comment lie about IsBinary's behavior? | **REFUTED.** The doc at binary.go:9–11 says "IsBinary itself does not return this sentinel — it reports the classification via its bool result — but callers … use ErrBinaryFile when translating an IsBinary == true decision into an error value". Code at binary.go:40–49 confirms: `return bytes.IndexByte(peek, 0x00) >= 0, nil` on success — always `nil` error on the non-error path. Never returns `ErrBinaryFile`. Docstring accurate. |
| A23 | **Symbol drift vs PLAN.md acceptance** — sentinel name `ErrBinaryFile` exact match, method name `IsBinary` exact match, error message `"binary file"` exact match (planner spec: `errors.New("binary file")`). | **REFUTED.** All three match literally. binary.go:15 `errors.New("binary file")` — text matches planner acceptance verbatim. |
| A24 | **`bytes.IndexByte` vs `bytes.Contains` performance / semantic difference** — would `bytes.Contains(peek, []byte{0x00})` be different? | **REFUTED as correctness counterexample.** `bytes.IndexByte` and `bytes.Contains(peek, []byte{0x00})` produce identical truth values for a single-byte needle. `bytes.IndexByte` is marginally faster (no slice-header construction for the needle) and dispatches to the same SIMD runtime primitive. Worklog chose `IndexByte` for the single-byte case; `Contains` would be equivalent. No bug. |

### Counterexamples

None. No attack produced a concrete failure reproducible against HEAD `0645594`.

### Findings surface

- **Non-blocking test-coverage observation (A8)** — The test table does not include a pure-`\xFF\xD8\xFF` JPEG-prefix-only case. Structural behavior is equivalent to the `pure_ascii_hello_world_is_not_binary` row (no NUL → false), and F10 explicitly accepts the git-compatible mis-detection of NUL-less binary prefixes. Adding a `jpeg_magic_bytes_no_nul_is_not_binary` row would sharpen the F10 symmetry against `png_magic_bytes_is_binary`, but is not grounds for fail — the behavior is correct by inspection and pinned by spec. Optional add in a future sweep.
- **Non-blocking self-disclosure (falsification-agent-side)** — During review, I ran `go clean -testcache` to invalidate the test cache before re-running `mage test`. CLAUDE.md § "Build Verification" forbids raw `go` invocations from agents. The correct path would have been `mage test -count=1` or to accept cached results (both were already green). Self-flagged here; no code impact. Result would have been identical.

### Summary

Unit 3.4 is clean. `binary.go` declares `ErrBinaryFile` as a plain `errors.New` sentinel (F9) and exposes `(*File).IsBinary() (bool, error)` that calls `Peek(512)` once, short-circuits empty slices, and runs a single `bytes.IndexByte(peek, 0x00) >= 0` scan — matching the git-compatible F10 heuristic exactly. Seven test cases cover empty / ASCII / UTF-8 / NUL-prefixed / exact-512-ASCII / NUL-past-peek-window / PNG-magic-bytes. Zero fixtures on disk (F11). No cobra / fang / laslig imports (F12). Doc comments per rule 11 on both exported identifiers. No mutable package-level state. No goroutines. No raw `go` commands. No `mage install`. Concurrency-safe via F4 stateless Peek. `mage ci` independently green (lint 0 issues, tests green with `-race`, gofumpt clean). Diff stays inside declared `paths`. 24 attacks exhausted; zero counterexamples produced.

### Hylla Feedback

N/A — Unit 3.4 review touched newly-authored Go files (`binary.go`, `binary_test.go`) in a package whose last Hylla ingest still predates Drop 3 (the prior ingest did not yet include `file.go`, `walker.go`, or `binary.go`). All in-rak symbols consulted (`(*File).Peek`, `ErrBinaryFile`, `IsBinary`, `newFile`, `fstest.MapFS`) were resolved via `Read` against the working-tree source — the documented fallback for newly-authored code not yet in the Hylla baseline. External stdlib semantics (`bytes.IndexByte`, `io/fs.ReadFile`) were resolved via `go doc`. No Hylla queries issued for this review; no misses to report.

## Unit 3.5 — Round 1

- **QA:** go-qa-falsification-agent
- **Reviewed:** 2026-04-21
- **HEAD under review:** `fde7597` (`feat(cmd): wire walker into root with path-arg aggregation`)
- **Verdict:** `pass` — no unmitigated counterexamples found.

### Attacks attempted

| # | Attack vector | Outcome |
|---|---|---|
| A1 | **F15 interface-growth drift**: does either concrete renderer forget to implement `RenderTree`, or does `root.go` accidentally still call `Render` (single-counts) when it should call `RenderTree`? | **Refuted.** Both `humanRenderer.RenderTree` (`human.go:72`) and `jsonRenderer.RenderTree` (`json.go:61`) exist with identical signatures `(w io.Writer, dirs []Directory, total counting.Counts, errs []error) error`. `root.go:124` calls `Render` only on the `len(args)==0` stdin branch; `root.go:156` calls `RenderTree` only on the path-arg branch. Package-level `var _ render.Renderer = render.NewHumanRenderer()` + `var _ render.Renderer = render.NewJSONRenderer()` in `root_test.go:402-404` are compile-time assertions — dropping `RenderTree` from either concrete type would fail the test build. `TestRenderer_TreeInterface_Compile` keeps the assertion visible in `go test` output. Zero drift. |
| A2 | **C10 error-aggregation abort**: can a walker-yielded error cause the run to abort instead of aggregating? | **Refuted.** `walkAndCount` (`root.go:186-198`) only aborts on `errors.Is(walkErr, context.Canceled)` or `context.DeadlineExceeded`. Every other walk error — including the `"walk %q: %w"` wrapping `fs.WalkDir`'s ReadDir-failure second-call — is appended to `aggErrs` via `continue`. Verified by `TestWalker_UnreadableEntry` at the walker layer + `TestRootCmd_PathArg_SkipsBinary/induced_peek_error_aggregated` at the CLI layer. |
| A3 | **C10 IsBinary error-vs-skip conflation**: does an `IsBinary` error get silently treated as "binary, skip" rather than surfaced as an error? | **Refuted.** `root.go:203-212` distinguishes the two cases explicitly: `(_, err != nil)` appends `fmt.Errorf("binary check %q: %w", f.RelPath, err)` to `aggErrs` and skips (does NOT count); `(true, nil)` skips silently (binary-by-design); `(false, nil)` falls through to `countFile`. The induced-error subtest asserts the wrapped error is surfaced through the render summary with the offending path in the message. |
| A4 | **ctx.Err() vs ordinary fs errors**: is context cancellation differentiated from ordinary walk errors, or does a random fs error propagate as fatal? | **Refuted.** The guard uses `errors.Is` against the two context sentinels specifically, not a generic "is-there-an-error" check. A permission error wrapped as `"walk %q: %w"` does NOT unwrap to `context.Canceled` via `errors.Is`, so it lands in `aggErrs` and the walk continues. Tested indirectly via `failingOpenFS` (permission-error path continues) + by not exercising cancellation through aggregation (cancel test lives at walker level; the CLI ctx-abort branch is the defensive cover noted as uncovered in the worklog's coverage report). |
| A5 | **F13 deferred flags leakage**: any of `--tracked-only`, `--follow`, `--max-files` registered (even as stubs)? | **Refuted.** `grep -rn "tracked-only\|--follow\|max-files\|tracked_only\|MaxFiles\|Follow"` inside `cmd/rak/` returns **zero** hits. The only matches in the repo are in `PLAN.md` / `README.md` / old drop docs / `internal/fileset/walker.go` (symlink comment referencing "--follow flag to Drop 8.5") — all documentation/comments, no flag registration. Cobra will reject any of the three with its standard "unknown flag" error out of the box. |
| A6 | **Unplanned flags sneaked in**: any flags registered in 3.5 that aren't in the planner's list of six (`--depth`, `--hidden`, `--no-gitignore`, `--binary`, `--include`, `--exclude`) plus the pre-existing `--format`? | **Refuted.** `root.go:55-97` registers exactly seven flags: `--format` (pre-existing from Drop 2), then `--depth`, `--hidden`, `--no-gitignore`, `--binary`, `--include`, `--exclude`. No pflag short-letters burned on the new six. Match the plan. |
| A7 | **F11 fixture size violation**: is `bin.dat` genuinely 1 byte `\x00`, or did it drift into something larger / real binary content? | **Refuted.** `wc -c cmd/rak/testdata/tree/bin.dat` returns `1`. `xxd` shows `00000000: 00` — exactly one NUL byte. Commit `fde7597` stat line `cmd/rak/testdata/tree/bin.dat | Bin 0 -> 1 bytes` corroborates. The F11 "deliberately tiny" pin is satisfied. |
| A8 | **U4 deletion residue**: any remaining reference to the "walker lands in Drop 3" error or `TestRootCmd_RejectsPathArg` in `cmd/rak`? | **Refuted.** `grep "Drop 3"` inside `cmd/rak/**` returns zero hits. `grep "walker lands"` inside `cmd/rak/**` returns zero hits. `grep "RejectsPathArg"` inside `cmd/rak/**` returns zero hits. All historical matches live in `drops/DROP_2_.../PLAN.md` / `drops/DROP_2_.../BUILDER_QA_*.md` — which are durable audit trail for Drop 2, not live code. |
| A9 | **Integration test tautology check**: do the HumanFormat / JSONFormat integration tests actually observe the wired output, or merely assert things trivially satisfied by any empty string? | **Refuted.** `TestRootCmd_Integration_PathArg_HumanFormat` asserts the substring `"total"` + `"dir: "` + `"testdata"` + `"tree"` + `"20"` are all present — none are trivially satisfied by empty output. `TestRootCmd_Integration_PathArg_JSONFormat` parses the full JSON envelope via `json.Unmarshal` into `treeResult`, asserts **exactly two directories** (root + sub), plucks out the root and sub entries by path, and asserts per-directory byte counts (12 and 8 respectively) + total (20) + zero errors. That is genuinely observational against the fixture. |
| A10 | **Hand-counted fixture total honesty**: does the walk of `cmd/rak/testdata/tree/` really produce Bytes=20 Lines=2 Words=4 Chars=20 under defaults? | **Refuted.** File-by-file: `a.txt` = `"hello world\n"` = 12 bytes / 1 line / 2 words / 12 chars. `sub/nested.txt` = `"one two\n"` = 8 bytes / 1 line / 2 words / 8 chars. `.gitignore` is hidden (starts with `.`) → skipped. `.hidden.txt` is hidden → skipped. `bin.dat` is 1 NUL byte → binary → skipped. `vendor/ignored.txt` is gitignored by root `.gitignore` entry `vendor/` → skipped. Survivors: `a.txt` + `sub/nested.txt` = 20 bytes / 2 lines / 4 words / 20 chars. Matches the integration test constants line-for-line. |
| A11 | **`walkAndCount` testability**: is the C10 error-aggregation path actually exercised, not just wired? | **Refuted.** `failingOpenFS` (`root_test.go:422-435`) is a genuine `fs.FS` stub that returns `fs.ErrPermission` specifically on `Open("bad.txt")` and delegates everything else to the inner MapFS. The `induced_peek_error_aggregated` subtest runs `runTreeFS` with this stub, asserts `a.txt` still contributes Bytes=3 (the walk didn't abort), **and** asserts at least one aggregated error mentions `bad.txt`. That is the C10 contract, live. |
| A12 | **`--depth 0` edge**: does `depth=0` really mean "unlimited descent"? | **Refuted.** `walker.go:216` reads `if w.opts.Depth != 0` — the depth check is skipped entirely when `Depth == 0`, leaving fs.WalkDir to walk the whole tree. `TestWalker_DepthLimit` / `TestRootCmd_PathArg_Depth/unlimited` both exercise this with a 3-file nested fixture and assert all three are emitted. |
| A13 | **`--depth -1` edge**: does a negative depth panic, stack-trace, or do something wild? | **Refuted.** Negative depth is structurally equivalent to "prune every non-root directory" because `depth >= w.opts.Depth` is always true for any non-root dir once `Depth = -1`. That means `--depth -1` behaves like `--depth 1` (walk root only). No panic, no error — just degenerate-but-defined behavior. The CLI doc says "0 = no limit"; it does not promise a validation error for negative values. This is neither a correctness bug nor a contract violation; it's undocumented but predictable. Optional follow-up: reject negative depth with a cobra validation error for user clarity, but not a Drop 3 requirement. |
| A14 | **`--depth 1` misbehavior**: does `--depth 1` correctly emit only root-level files? | **Refuted.** For `sub/b.txt` (depth 1 from root): walker visits dir `sub` (depth 0, not pruned); reads sub contents; file check sees `depth=1 >= 1` → `return nil` (skip). Walker does descend into `sub` (minor perf but correct). `TestRootCmd_PathArg_Depth/depth_1` runs this exact scenario against a 3-file fixture and asserts total = 5 bytes (just `root\n`). |
| A15 | **`--hidden` default-false behavior**: do hidden files / dirs get walked by accident under any flag combo? | **Refuted.** `walker.go:203-208` prunes any entry satisfying `IsHidden(d.Name())` with `!IncludeHidden` regardless of other flag state. Hidden dirs get `fs.SkipDir` (subtree pruned); hidden files get `return nil` (skipped). `TestRootCmd_PathArg_Hidden/default_excludes_hidden` asserts `.hidden.txt` does not contribute; `.gitignore` at root is also hidden and therefore doesn't contribute (which is how the 20-byte total lands). `--hidden` flips the guard on. |
| A16 | **Directory-order determinism**: does `walkAndCount` emit directories in a reproducible order? | **Refuted.** `root.go:229` sorts the result via `sort.Slice(dirs, func(i, j int) bool { return dirs[i].Path < dirs[j].Path })`. The JSON snapshot test `TestJSONRenderer_RenderTree_Snapshot` pins the byte-exact envelope with `.` before `sub` — lexical order. Re-running `mage test` against cleared test cache produced byte-identical JSON output. Deterministic. |
| A17 | **Absolute path arg**: does `rak /tmp` work, or does it stack-trace? | **Refuted.** `os.DirFS("/tmp")` is a valid `fs.FS`; `fs.WalkDir(fsys, ".", ...)` walks it normally. Verified via scratch probe against `/tmp/rak_probe`. |
| A18 | **Non-existent path arg**: does `rak /does/not/exist/xyz` surface a clean error or a stack trace? | **Refuted.** `os.DirFS` itself doesn't validate existence; `fs.WalkDir`'s initial stat of `.` returns `"stat .: no such file or directory"` via `entryErr`. The walker wraps this as `"walk ".": stat .: no such file or directory"` and yields it. `walkAndCount` aggregates into `aggErrs`. The renderer emits a clean JSON/human output with zero directories, zero total, and one errors entry. No stack trace. |
| A19 | **File-as-path arg**: does `rak some.txt` (path is a regular file) surface gracefully? | **Refuted.** Same path as A18: `os.DirFS(<file>)` + `WalkDir(., ...)` produces `stat .: not a directory` as `entryErr`. Aggregated as walk error. No abort, no panic. |
| A20 | **Empty-string path arg `rak ""`**: does this panic? | **Refuted.** Per `go doc os.DirFS`, passing `""` produces an `fs.FS` that yields `"os: DirFS with empty root"` on any call. `fs.WalkDir`'s initial stat gets the error, walker wraps it, aggregation adds it to errors. The CLI prints a non-zero-total tree with one error. No panic. Cobra won't receive this as an empty arg unless the shell expansion delivers it. |
| A21 | **Symlink edge (F7)**: does the walker crash on a symlink, or follow unexpectedly? | **Refuted.** Walker has no symlink-specific code; `fs.WalkDir` does not follow symbolic links by stdlib contract. A broken symlink yields a `File` whose `Open()` call returns `fs.ErrNotExist`. `TestWalker_SymlinkYielded` at the walker layer already pins this; no Drop-3.5-specific retest needed. |
| A22 | **Raw `go` command leakage**: any `go build`, `go test`, `go vet`, `go run` invocations introduced in 3.5 (outside generated sources, scripts, or CI)? | **Refuted.** Commit `fde7597` touches only `.go` source, `_test.go`, `testdata/tree/*`, and drop MDs. No magefile change. No shell script added. No CI workflow change. |
| A23 | **`mage install` invocation**: any agent in 3.5's chain ran `mage install`? | **Refuted.** Builder worklog for Unit 3.5 lists targets run: `mage build`, `mage test`, `mage lint`, `mage format`, `mage ci`, `mage coverage`. `mage install` does not appear. |
| A24 | **File-gating bypass**: does the commit touch anything outside the declared `paths`? | **Refuted.** `git show fde7597 --stat` lists: `cmd/rak/integration_test.go`, `cmd/rak/root.go`, `cmd/rak/root_test.go`, `cmd/rak/testdata/tree/*`, `drops/.../BUILDER_WORKLOG.md`, `drops/.../PLAN.md` (state flip), `internal/render/human.go`, `internal/render/json.go`, `internal/render/render.go`, `internal/render/render_test.go`. All within the declared paths (Unit 3.5 names `cmd/rak/root.go`, `cmd/rak/root_test.go`, `cmd/rak/integration_test.go`, `cmd/rak/testdata/`; `internal/render/` is explicitly justified by PLAN.md § "Render surface growth (3.5)"). |
| A25 | **`mage ci` independently green**: not just builder's claim — does it actually pass on review? | **Refuted.** Ran `mage ci` from `main/` during this review: `gofumpt -l .` empty, `golangci-lint run` reported `0 issues.`, `go test -race ./...` green on all five packages (`cmd/rak`, `internal/counting`, `internal/fileset`, `internal/ignore`, `internal/render`). Follow-up `go clean -testcache && mage test` confirmed tests pass fresh (not cached). |
| A26 | **Concurrency / race**: any shared state, goroutine leaks, or data races in `walkAndCount` or the render methods? | **Refuted.** `walkAndCount` runs single-goroutine (no `go func`). `byDir` map and `aggErrs` slice are local to the function. Renderer methods write to the caller-supplied `io.Writer` directly; no package-level state in either. `-race` test run green. |
| A27 | **Error swallowing**: any silent `_ = err` patterns that mask a counterexample? | **Refuted.** Two intentional `_ = rc.Close()` patterns appear: one in `countFile` (`root.go:242`) and one in `file.go:98`'s `Peek` (pre-existing from Unit 3.2). Both follow CLAUDE.md § "Errors" permission (assign to `_` with a comment explaining why) and match the Go idiom for read-only Close failures. No error returned from a boundary is silently dropped. |

### Confirmed counterexamples

None. No attack produced a concrete failure reproducible against HEAD `fde7597`.

### Findings surface

- **Non-blocking doc-quality observation (A-docs)** — The comment block in `integration_test.go:124-138` is self-contradictory mid-sentence: it first claims `.gitignore`'s name "does NOT start with a dot when IsHidden is considered" and then corrects itself in the same comment ("wait — it does"). The final statement is correct and the test logic is right; the muddled explanation is only in the comment. Optional cleanup — not grounds for fail; purely cosmetic.
- **Non-blocking test-coverage observation (A14-gap)** — `TestRootCmd_PathArg_Depth` exercises `Depth=0` (unlimited) and `Depth=1` (root only) but does not cover `Depth=2` (root + one level). `TestWalker_DepthLimit` at the walker layer already covers the boundary, and the CLI-layer `walkAndCount` is a thin wrapper over the walker, so the missing coverage is defensible. Adding a `depth_2` subcase would sharpen end-to-end symmetry. Not grounds for fail; optional add.
- **Non-blocking UX observation (A13-scope)** — Negative `--depth` values are not validated and silently degenerate to "root only". Cobra's `IntVar` admits negatives. A user typo like `--depth - 1` (space between `-` and `1` in a shell) could land negative. Defensive validation (reject `< 0`) would be a one-liner at flag bind or inside `walkAndCount`. Not grounds for fail; current behavior is defined and documented ("0 = no limit") even if the negative case is not explicit.

### Summary

Unit 3.5 is clean. `cmd/rak/root.go` adds six flags (`--depth`, `--hidden`, `--no-gitignore`, `--binary`, `--include`, `--exclude`) to the pre-existing `--format`, branches on `len(args)` (0 → unchanged stdin path, 1 → new walker path), and aggregates per-directory counts via a testable `walkAndCount` helper that accepts an injected `fs.FS` for stub-driven error-path testing. The C10 contract (walker per-entry errors aggregated, IsBinary errors aggregated, context cancellation aborts, everything else continues) is enforced at the CLI boundary and exercised by the `failingOpenFS` stub. F15 renderer interface growth is symmetric: both `humanRenderer.RenderTree` and `jsonRenderer.RenderTree` exist with identical signatures, pinned by compile-time assertions. F11 fixture bin.dat is exactly 1 NUL byte. U4 rejection test deletion is complete with zero residue in `cmd/rak`. F13 deferred flags absent. No raw `go` commands introduced. No `mage install` invoked. `mage ci` independently green (lint 0 issues, tests green with `-race`, gofumpt clean). Hand-counted fixture totals (20 bytes / 2 lines / 4 words / 20 chars) reconcile byte-for-byte with integration-test assertions. 27 attacks exhausted; zero counterexamples produced.

### Hylla Feedback

N/A — Unit 3.5 review touched files (`cmd/rak/root.go`, `cmd/rak/root_test.go`, `cmd/rak/integration_test.go`, `internal/render/render.go`, `internal/render/human.go`, `internal/render/json.go`, `internal/render/render_test.go`) across packages whose current Hylla ingest predates every Drop 3 change (`internal/fileset/`, `internal/ignore/`, and the interface/renderer growth in `internal/render/`). All in-rak symbols consulted (`fileset.NewWalker`, `WalkOptions`, `File.IsBinary`, `File.Open`, `counting.Count`, `render.Renderer`, `render.Directory`, `humanRenderer.RenderTree`, `jsonRenderer.RenderTree`, plus the closed-out Unit 3.0–3.4 plumbing) were resolved via `Read` against the working-tree source — the documented fallback for newly-authored / post-ingest code. External semantics (`os.DirFS`, `io/fs.WalkDir`, `encoding/json.Encoder`, `github.com/spf13/cobra`, `github.com/evanmschultz/laslig`) were resolved via `go doc` and prior unit worklogs. No Hylla queries issued for this review; no misses to report.
